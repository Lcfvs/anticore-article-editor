/* {
  "name": "anticore-article-editor",
  "version": "1.0.0",
  "author": "lcf.vs <michael.rouges@gmail.com>",
  "license": "UNLICENSED",
  "dependencies": {
    "URL": {
      "name": "js-polyfills",
      "version": "0.1.42",
      "author": "Joshua Bell",
      "license": "Unlicense",
      "homepage": "https://github.com/inexorabletash/polyfill"
    },
    "Promise": {
      "name": "es6-promise",
      "version": "4.2.4",
      "author": "Yehuda Katz, Tom Dale, Stefan Penner and contributors",
      "license": "MIT",
      "homepage": "https://github.com/stefanpenner/es6-promise#readme"
    },
    "fetch": {
      "name": "whatwg-fetch",
      "version": "2.0.3",
      "license": "MIT",
      "homepage": "https://github.com/github/fetch#readme"
    },
    "anticore": {
      "name": "anticore",
      "version": "2.1.7",
      "author": "Lcf.vs",
      "license": "MIT",
      "homepage": "https://github.com/Lcfvs/anticore#readme"
    }
  }
} */
/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, {
/******/ 				configurable: false,
/******/ 				enumerable: true,
/******/ 				get: getter
/******/ 			});
/******/ 		}
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 91);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.anticore = undefined;

var _getTarget = __webpack_require__(60);

var _prevent = __webpack_require__(1);

var _global = __webpack_require__(9);

var _document = __webpack_require__(20);

var _all = __webpack_require__(16);

var _create = __webpack_require__(14);

var _keys = __webpack_require__(61);

var _forEach = __webpack_require__(21);

var _indexOf = __webpack_require__(10);

var _toDOM = __webpack_require__(97);

var _remove = __webpack_require__(5);

var _onClick = __webpack_require__(2);

var _onSubmit = __webpack_require__(66);

var _nodeName = __webpack_require__(18);

var _parent = __webpack_require__(3);

var _rects = __webpack_require__(101);

var _request = __webpack_require__(102);

var _queue = __webpack_require__(67);

var _one = __webpack_require__(6);

var anticore = exports.anticore = (0, _create.create)(); /*
                                                          Copyright MIT 2017 Lcf.vs
                                                          https://github.com/Lcfvs/anticore
                                                          */

var window = (0, _global.global)(),
    encodeURIComponent = window.encodeURIComponent,
    fetch = window.fetch,
    URL = window.URL,
    FormData = window.FormData,
    registry = (0, _create.create)(),
    types = ['html', 'svg', 'xml'],
    selector = 'input[type=submit]:focus,' + 'button[type=submit]:focus,' + 'button:not([type]):focus,' + 'input[type=submit]:hover,' + 'button[type=submit]:hover,' + 'button:not([type]):hover,' + 'input[name]:not([type=file]):not([type=reset]):not([type=submit]):not([type=checkbox]):not([type=radio]):not(:disabled),' + 'input[name][type=checkbox]:checked:not(:disabled),' + 'input[name][type=radio]:checked:not(:disabled),' + 'textarea[name]:not(:disabled),' + 'select[name]:not(:disabled) [selected=selected]';

anticore.fetchers = (0, _create.create)();
anticore.request = _request.request;

/**
 * Builds a request based on an anchor
 * @param {HTMLAnchorElement} a
 * @returns {Object} request
 */
anticore.fetchers.a = function (a) {
  return (0, _request.request)(a.href, 'get', null, a);
};

/**
 * Builds a request based on a form
 * @param {HTMLFormElement} form
 * @returns {Object} request
 */
anticore.fetchers.form = function (form) {
  var action = new URL(form.action || form.ownerDocument.location.href),
      method = form.method,
      data = void 0;

  if (method === 'post') {
    data = new FormData(form);
  } else {
    action.search += (0, _indexOf.indexOf)(action.search, '?') > -1 ? '' : '?';
    (0, _forEach.forEach)((0, _all.all)(selector, form), stringify, action);
  }

  return (0, _request.request)(action, method, data, form);
};

/**
 * Builds a request based on an element
 * (form or anchor, for more, extend anticore fetchers)
 * @param {HTMLElement} element
 * @returns {Object} request
 */
anticore.fetcher = function (element) {
  return anticore.fetchers[(0, _nodeName.nodeName)(element)](element);
};

/**
 * Populates the request response
 * @param {Object} request
 * @returns {Object} anticore
 */
anticore.trigger = function (request) {
  if (anticore.onTimeout(request)) {
    return anticore;
  }

  populate(request.response.result, true).then(request.resolve).then(function () {
    return request;
  });

  return anticore;
};

/**
 * Adds a listener, based on a querySelectorAll
 * @param {String} selector
 * @param {Function} middleware
 * @returns {Object} anticore
 */
anticore.on = function (selector, middleware) {
  registry[selector] = registry[selector] || [];

  if ((0, _indexOf.indexOf)(registry[selector], middleware) < 0) {
    registry[selector].push(middleware);
  }

  return anticore;
};

/**
 * Handles any requests timeout & retry if any
 * @param {Object} request
 * @returns {Boolean}
 */
anticore.onTimeout = function (request) {
  if (request.response.status === 408) {
    request.retry();

    return true;
  }

  return false;
};

/**
 * Launches the selectors tests to find the related listeners,
 * takes the scoped document if not passed as argument
 * @param {Document|HTMLElement|DocumentFragment} [container=global.document)
 * @returns {Object} anticore
 */
anticore.populate = function (container) {
  return populate(container || (0, _document.document)());
};

/**
 * Builds a request
 * @param {String} url
 * @param {String} method (get or post)
 * @param {Object} [body] (the post request body)
 * @param {Element} [target] (the event target)
 * @return {Object}
 */
anticore.request = function (url, method, body, target) {
  var instance = (0, _request.request)(url, method, body, target);

  instance.fetchRequest = fetchRequest;

  return instance;
};

/**
 * Default fetcher listener
 * @param {Event} event
 */
anticore.fetchFromEvent = function (event) {
  if (event.defaultPrevented) {
    return false;
  }

  var target = (0, _getTarget.getTarget)(event),
      request = anticore.fetcher(target);

  request.target = target;
  request.originalTarget = (0, _one.one)(selector, target.ownerDocument);
  request.fetchRequest = fetchRequest;
  (0, _prevent.prevent)(event);

  request.fetch(anticore.trigger)['catch'](anticore.onError);

  return false;
};

anticore.onError = console.error.bind(console);

/**
 * Adds default a:not([download]):not([target]):not([href^="data:"]),a[target=_self]:not([download]):not([href^="data:"]) & form middlewares
 * @returns {Object} anticore
 */
anticore.defaults = function () {
  anticore.on('a:not([download]):not([target]):not([href^="data:"]),a[target=_self]:not([download]):not([href^="data:"])', function (element, next) {
    (0, _onClick.onClick)(element, anticore.fetchFromEvent);
    next();
  });

  anticore.on('form:not([target]),form[target=_self]', function (element, next) {
    (0, _onSubmit.onSubmit)(element, cleanAndFetch);
    next();
  });

  return anticore;
};

function stringify(item) {
  if (!item.offsetWidth && !item.offsetHeight && !(0, _rects.rects)(item).length) {
    return;
  }

  this.search += '&' + encodeURIComponent(((0, _nodeName.nodeName)(item) === 'option' ? (0, _parent.parent)(item) : item).name) + '=' + encodeURIComponent(item.value).replace(/%20/g, '+');
}

function notify(response) {
  var target = _queue.queue[0].request.originalTarget;

  if (target) {
    target.classList.toggle('fetching');
  }

  return response;
}

function fetchRequest() {
  var item = _queue.queue[0];

  if (_queue.queue[1]) {
    return;
  }

  notify();

  return fetch(item.request.url, item.request.options).then(onResponse).then(notify).then(onFragment).then(item.trigger || item.request.resolve).then(_queue.queue.next)['catch'](item.reject);
}

function onResponse(response) {
  var type = ((response.headers.get('content-type') || 'application/octet-stream').match(/json|html|svg|xml|text(?=\/plain)/) || ['blob'])[0],
      item = _queue.queue[0];

  item.type = type;
  item.request.response = response;

  return response[(0, _indexOf.indexOf)(types, type) > -1 ? 'text' : type]();
}

function cleanAndFetch(event) {
  (0, _forEach.forEach)((0, _all.all)('.error', (0, _getTarget.getTarget)(event)), _remove.remove);
  anticore.fetchFromEvent(event);
}

function onFragment(data) {
  var item = _queue.queue[0];

  if ((0, _indexOf.indexOf)(types, item.type) > -1) {
    item.request.response.result = (0, _toDOM.toDOM)(data);
  } else {
    item.request.response.result = data;
  }

  return item.request;
}

function nextRecord(resolve) {
  var record = this.shift();

  if (!record) {
    return resolve && resolve();
  }

  record[0](record[1], this.next, this.loaded);
}

function onSelector(selector) {
  var queue = this,
      nodes = (0, _all.all)(selector, queue.container);

  queue.selector = selector;
  (0, _forEach.forEach)(nodes, onElement, queue);
}

function onElement(element) {
  var queue = this;

  queue.element = element;
  (0, _forEach.forEach)(registry[queue.selector], onListener, queue);
}

function onListener(listener) {
  this.push([listener, this.element, this.loaded]);
}

function populate(container, loaded) {
  return new Promise(function (resolve) {
    var queue = [];

    queue.container = container;
    queue.loaded = loaded;
    (0, _forEach.forEach)((0, _keys.keys)(registry), onSelector, queue);
    queue.next = nextRecord.bind(queue, resolve);
    queue.next();
  });
}

/***/ }),
/* 1 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.prevent = prevent;
function prevent(event) {
  event.preventDefault();

  return event;
}

/***/ }),
/* 2 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onClick = onClick;

var _ = __webpack_require__(8);

function onClick(element, listener, useCapture) {
  return (0, _.on)('click', element, listener, useCapture);
}

/***/ }),
/* 3 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.parent = parent;
function parent(node) {
  return node.parentNode;
}

/***/ }),
/* 4 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.current = undefined;

var _global = __webpack_require__(9);

var current = exports.current = (0, _global.global)().getSelection;

/***/ }),
/* 5 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.remove = remove;

var _parent = __webpack_require__(3);

function remove(node) {
  if ((0, _parent.parent)(node)) {
    (0, _parent.parent)(node).removeChild(node);
  }

  return node;
}

/***/ }),
/* 6 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.one = one;

var _document = __webpack_require__(20);

function one(selector, refNode) {
  refNode = refNode || (0, _document.document)();

  return selector === undefined ? refNode : refNode.querySelector(selector);
}

/***/ }),
/* 7 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.clean = clean;

var _remove = __webpack_require__(5);

var _returns = __webpack_require__(113);

var _styles = __webpack_require__(114);

var _isEmpty = __webpack_require__(25);

var _isTrailingStyle = __webpack_require__(115);

var _isTrailingReturn = __webpack_require__(116);

var _forEach = __webpack_require__(21);

function clean(node) {
  (0, _forEach.forEach)((0, _styles.styles)(node), removeTrailingStyle);
  (0, _forEach.forEach)((0, _returns.returns)(node), removeTrailingReturn);

  return (0, _isEmpty.isEmpty)(node);
}

function removeTrailingStyle(node) {
  if ((0, _isTrailingStyle.isTrailingStyle)(node)) {
    (0, _remove.remove)(node);
  }

  node.normalize();
}

function removeTrailingReturn(node) {
  if ((0, _isTrailingReturn.isTrailingReturn)(node)) {
    (0, _remove.remove)(node);
  }

  node.normalize();
}

/***/ }),
/* 8 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.on = undefined;

var _callEach = __webpack_require__(65);

var _global = __webpack_require__(9);

/**
 * Listens an event on an element (touch or not)
 * @param {String} event
 * @param {Element} element
 * @param {Function} listener
 * @param {Boolean} useCapture
 * @returns {Function} realListener
 */
var on = exports.on = _callEach.callEach.bind(null, (0, _global.global)().Element.prototype.addEventListener);

/***/ }),
/* 9 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
var global = exports.global = new Function('return this');

/***/ }),
/* 10 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.indexOf = undefined;

var _demethodize = __webpack_require__(32);

var indexOf = exports.indexOf = (0, _demethodize.demethodize)(Array.prototype.indexOf);

/***/ }),
/* 11 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.closest = closest;

var _matches = __webpack_require__(23);

var _parent = __webpack_require__(3);

function closest(selector, node) {
  var last = node,
      current = void 0;

  while (current = (0, _parent.parent)(last)) {
    if ((0, _matches.matches)(selector, current)) {
      return current;
    }

    last = current;
  }

  return null;
}

/***/ }),
/* 12 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.text = text;

var _isElement = __webpack_require__(29);

var _isUndefined = __webpack_require__(64);

function text(node, data) {
  if ((0, _isElement.isElement)(node)) {
    if (!(0, _isUndefined.isUndefined)(data)) {
      node.textContent = data;
    }

    return node.textContent;
  }

  if (!(0, _isUndefined.isUndefined)(data)) {
    node.nodeValue = data;
  }

  return node.nodeValue;
}

/***/ }),
/* 13 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.curry = curry;
function curry(fn) {
  for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    args[_key - 1] = arguments[_key];
  }

  return fn.bind.apply(fn, [this].concat(args));
}

/***/ }),
/* 14 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
var create = exports.create = function (method, prototype, descriptors) {
  return method(prototype || null, descriptors);
}.bind(null, Object.create);

/***/ }),
/* 15 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.before = before;

var _parent = __webpack_require__(3);

function before(node, refNode, parentNode) {
  if (refNode) {
    (0, _parent.parent)(refNode).insertBefore(node, refNode);
  } else {
    parentNode.appendChild(node);
  }

  return node;
}

/***/ }),
/* 16 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.all = all;

var _document = __webpack_require__(20);

function all(selector, refNode) {
  return (refNode || (0, _document.document)()).querySelectorAll(selector);
}

/***/ }),
/* 17 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.nodes = nodes;
function nodes(node) {
  return node.childNodes;
}

/***/ }),
/* 18 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.nodeName = nodeName;
function nodeName(node) {
  if (!node) {
    return null;
  }

  return node.nodeName.toLowerCase();
}

/***/ }),
/* 19 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.append = append;

var _before = __webpack_require__(15);

function append(node, parent) {
  return (0, _before.before)(node, null, parent);
}

/***/ }),
/* 20 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.document = document;

var _global = __webpack_require__(9);

function document() {
  return (0, _global.global)().document;
}

/***/ }),
/* 21 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.forEach = undefined;

var _demethodize = __webpack_require__(32);

var forEach = exports.forEach = (0, _demethodize.demethodize)(Array.prototype.forEach);

/***/ }),
/* 22 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.element = element;

var _document = __webpack_require__(20);

function element(tag) {
  return (0, _document.document)().createElement(tag);
}

/***/ }),
/* 23 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.matches = matches;

var _indexOf = __webpack_require__(10);

var _isText = __webpack_require__(24);

var _all = __webpack_require__(16);

var _parent = __webpack_require__(3);

function matches(selector, node) {
  var results = (0, _all.all)(selector, node.document || node.ownerDocument),
      i = results.length;

  if ((0, _isText.isText)(node)) {
    node = (0, _parent.parent)(node);
  }

  while (--i >= 0 && results.item(i) !== node) {}

  return i > -1;
}

/***/ }),
/* 24 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isText = isText;
function isText(node) {
  return node.nodeType === 3;
}

/***/ }),
/* 25 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isEmpty = isEmpty;

var _text = __webpack_require__(12);

var _isElement = __webpack_require__(29);

var _one = __webpack_require__(6);

function isEmpty(node) {
  return (!(0, _isElement.isElement)(node) || !(0, _one.one)('*', node)) && !((0, _text.text)(node) || '').trim().length;
}

/***/ }),
/* 26 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onEnter = onEnter;

var _ = __webpack_require__(8);

function onEnter(element, listener, useCapture) {
  return (0, _.on)('keydown', element, function (event) {
    if (event.which === 13 && !event.shiftKey && !event.ctrlKey) {
      return listener.call(this, event);
    }
  }, useCapture);
}

/***/ }),
/* 27 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.editables = editables;

var _all = __webpack_require__(16);

var _closest = __webpack_require__(11);

function editables(element) {
  return (0, _all.all)('[contenteditable="true"]', (0, _closest.closest)('article', element));
}

/***/ }),
/* 28 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.appendAll = appendAll;

var _append = __webpack_require__(19);

var _forEach = __webpack_require__(21);

var _slice = __webpack_require__(47);

function appendAll(nodes, parent) {
  (0, _forEach.forEach)((0, _slice.slice)(nodes, 0), function (node) {
    (0, _append.append)(node, parent);
  });

  return nodes;
}

/***/ }),
/* 29 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isElement = isElement;
function isElement(node) {
  return node.nodeType === 1;
}

/***/ }),
/* 30 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isHeadingNode = isHeadingNode;

var _isElement = __webpack_require__(29);

var _matches = __webpack_require__(23);

var _closest = __webpack_require__(11);

var selector = 'h1, h2, h3, h4, h5, h6';

function isHeadingNode(node) {
  return node && (0, _isElement.isElement)(node) && (0, _matches.matches)(selector, node) || (0, _closest.closest)(selector, node);
}

/***/ }),
/* 31 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.attr = attr;

var _isBoolean = __webpack_require__(69);

var _isString = __webpack_require__(41);

var _isNull = __webpack_require__(50);

var _isNumber = __webpack_require__(42);

var _camelToDash = __webpack_require__(70);

function attr(element, name, value) {
  name = (0, _camelToDash.camelToDash)(name);

  if ((0, _isString.isString)(value) || (0, _isNumber.isNumber)(value) || (0, _isBoolean.isBoolean)(value)) {
    element.setAttribute(name, value);

    return element;
  }

  if ((0, _isNull.isNull)(value)) {
    element.removeAttribute(name);

    return element;
  }

  return element.getAttribute(name);
}

/***/ }),
/* 32 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
var demethodize = exports.demethodize = Function.bind.bind(Function.call);

/***/ }),
/* 33 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.anchor = undefined;

var _create = __webpack_require__(14);

var _current = __webpack_require__(4);

var anchor = exports.anchor = (0, _create.create)();

anchor.node = function () {
  return (0, _current.current)().anchorNode;
};

anchor.offset = function () {
  return (0, _current.current)().anchorOffset;
};

/***/ }),
/* 34 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isCollapsed = isCollapsed;

var _current = __webpack_require__(4);

function isCollapsed() {
  return (0, _current.current)().isCollapsed;
}

/***/ }),
/* 35 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.closestOrSelf = closestOrSelf;

var _isElement = __webpack_require__(29);

var _matches = __webpack_require__(23);

var _closest = __webpack_require__(11);

function closestOrSelf(selector, node) {
  return (0, _isElement.isElement)(node) && (0, _matches.matches)(selector, node) ? node : (0, _closest.closest)(selector, node);
}

/***/ }),
/* 36 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.update = update;

var _isObject = __webpack_require__(130);

var _keys = __webpack_require__(61);

var _on = __webpack_require__(8);

var _forEach = __webpack_require__(21);

var _append = __webpack_require__(19);

var _before = __webpack_require__(15);

var _text = __webpack_require__(12);

var _attr = __webpack_require__(31);

var _style = __webpack_require__(131);

var _data = __webpack_require__(43);

function update(element, config) {
  if ((0, _isObject.isObject)(config)) {
    (0, _forEach.forEach)((0, _keys.keys)(config), function (name) {
      var value = config[name];

      if (name.substr(0, 2) === 'on') {
        (0, _on.on)(name.substr(2).toLowerCase(), element, value);
      } else if (name === 'style' && (0, _isObject.isObject)(value)) {
        (0, _forEach.forEach)((0, _keys.keys)(value), function (name) {
          (0, _style.style)(element, name, value[name]);
        });
      } else if (name === 'dataset' && (0, _isObject.isObject)(value)) {
        (0, _forEach.forEach)((0, _keys.keys)(value), function (name) {
          (0, _data.data)(element, name, value[name]);
        });
      } else if (name === 'text') {
        (0, _text.text)(element, value);
      } else if (name === 'parent') {
        (0, _append.append)(element, value);
      } else if (name === 'next') {
        (0, _before.before)(element, value);
      } else {
        (0, _attr.attr)(element, name, value);
      }
    });
  }

  return element;
}

/***/ }),
/* 37 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.firstNode = firstNode;
function firstNode(node) {
  return node.firstChild;
}

/***/ }),
/* 38 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.promise = promise;

var _demethodize = __webpack_require__(32);

var _curry = __webpack_require__(13);

var demethodizedCurry = (0, _demethodize.demethodize)(_curry.curry);

function promise(fn) {
  for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    args[_key - 1] = arguments[_key];
  }

  return new Promise(demethodizedCurry.apply(undefined, [this, fn].concat(args)));
}

/***/ }),
/* 39 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.listenP = listenP;

var _onBack = __webpack_require__(51);

var _onBlur = __webpack_require__(56);

var _onDelete = __webpack_require__(83);

var _onEnter = __webpack_require__(26);

var _onLeft = __webpack_require__(84);

var _onRight = __webpack_require__(85);

var _onBlurEvent = __webpack_require__(152);

var _onEnterEvent = __webpack_require__(153);

var _onBackEvent = __webpack_require__(155);

var _onDeleteEvent = __webpack_require__(157);

var _onLeftEvent = __webpack_require__(159);

var _onRightEvent = __webpack_require__(160);

function listenP(element) {
  (0, _onBlur.onBlur)(element, _onBlurEvent.onBlurEvent, true);
  (0, _onEnter.onEnter)(element, _onEnterEvent.onEnterEvent, true);
  (0, _onBack.onBack)(element, _onBackEvent.onBackEvent, true);
  (0, _onDelete.onDelete)(element, _onDeleteEvent.onDeleteEvent, true);
  (0, _onLeft.onLeft)(element, _onLeftEvent.onLeftEvent, true);
  (0, _onRight.onRight)(element, _onRightEvent.onRightEvent, true);

  return element;
}

/***/ }),
/* 40 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.nextElement = nextElement;
function nextElement(node) {
  return node.nextElementSibling;
}

/***/ }),
/* 41 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isString = isString;
function isString(value) {
  return typeof value === 'string';
}

/***/ }),
/* 42 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isNumber = isNumber;
function isNumber(value) {
  return typeof value === 'number';
}

/***/ }),
/* 43 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.data = data;

var _isString = __webpack_require__(41);

var _isNull = __webpack_require__(50);

var _isNumber = __webpack_require__(42);

var _dashToCamel = __webpack_require__(71);

function data(element, name, value) {
  name = (0, _dashToCamel.dashToCamel)(name);

  if ((0, _isString.isString)(value) || (0, _isNumber.isNumber)(value)) {
    element.dataset[name] = value;

    return element;
  }

  if ((0, _isNull.isNull)(value)) {
    delete element.dataset[name];

    return element;
  }

  return element.dataset[name];
}

/***/ }),
/* 44 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isFirstP = isFirstP;

var _isText = __webpack_require__(24);

var _matches = __webpack_require__(23);

var _closest = __webpack_require__(11);

var _nodeName = __webpack_require__(18);

var _parent = __webpack_require__(3);

var _firstElement = __webpack_require__(86);

function isFirstP(node) {
  if (!node) {
    return false;
  }

  if ((0, _isText.isText)(node)) {
    node = (0, _closest.closest)('p', node);
  }

  return (0, _nodeName.nodeName)(node) === 'p' && (0, _firstElement.firstElement)((0, _parent.parent)(node)) === node;
}

/***/ }),
/* 45 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.starts = starts;

var _current = __webpack_require__(4);

var _parent = __webpack_require__(3);

var _contains = __webpack_require__(87);

var _firstNode = __webpack_require__(37);

function starts(node) {
  var selection = (0, _current.current)(),
      anchor = selection.anchorNode,
      offset = selection.anchorOffset;

  if (offset || !(0, _contains.contains)(anchor, node)) {
    return false;
  }

  while (anchor) {
    if ((0, _firstNode.firstNode)((0, _parent.parent)(anchor)) !== anchor) {
      return false;
    }

    if (anchor === node || anchor === (0, _firstNode.firstNode)(node)) {
      return true;
    }

    anchor = (0, _parent.parent)(anchor);
  }

  return false;
}

/***/ }),
/* 46 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.previous = previous;

var _indexOf = __webpack_require__(10);

var _end = __webpack_require__(58);

var _parent = __webpack_require__(3);

function previous(node, targets) {
  var key = (0, _indexOf.indexOf)(targets, node);

  while (node = targets[key -= 1]) {
    if ((0, _parent.parent)(node)) {
      return (0, _end.end)(node);
    }
  }
}

/***/ }),
/* 47 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.slice = undefined;

var _demethodize = __webpack_require__(32);

var slice = exports.slice = (0, _demethodize.demethodize)(Array.prototype.slice);

/***/ }),
/* 48 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.html = html;

var _isElement = __webpack_require__(29);

var _isUndefined = __webpack_require__(64);

function html(element, data) {
  if ((0, _isElement.isElement)(element)) {
    if (!(0, _isUndefined.isUndefined)(data)) {
      element.innerHTML = data;

      return element;
    }

    return element.innerHTML;
  }
}

/***/ }),
/* 49 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.nextNode = nextNode;
function nextNode(node) {
  return node.nextSibling;
}

/***/ }),
/* 50 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isNull = isNull;
function isNull(value) {
  return value === null;
}

/***/ }),
/* 51 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onBack = onBack;

var _ = __webpack_require__(8);

function onBack(element, listener, useCapture) {
  return (0, _.on)('keydown', element, function (event) {
    if (event.which === 8 && !event.shiftKey && !event.ctrlKey) {
      return listener.call(this, event);
    }
  }, useCapture);
}

/***/ }),
/* 52 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.next = next;

var _indexOf = __webpack_require__(10);

var _start = __webpack_require__(53);

var _parent = __webpack_require__(3);

function next(node, targets) {
  var key = (0, _indexOf.indexOf)(targets, node);

  while (node = targets[key += 1]) {
    if ((0, _parent.parent)(node)) {
      return (0, _start.start)(node);
    }
  }
}

/***/ }),
/* 53 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.start = start;

var _firstOrSelf = __webpack_require__(134);

var _select = __webpack_require__(74);

function start(node) {
  var first = (0, _firstOrSelf.firstOrSelf)(node);

  (0, _select.select)(first, 0);

  return first;
}

/***/ }),
/* 54 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.show = show;

var _index = __webpack_require__(1);

var _index2 = __webpack_require__(6);

var _index3 = __webpack_require__(19);

function show(tooltip, event) {
  if (event) {
    (0, _index.prevent)(event);
  }

  (0, _index3.append)(tooltip.element, (0, _index2.one)('main'));
  focus(tooltip.inputs.text);
}

/***/ }),
/* 55 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.nextOrClose = nextOrClose;

var _index = __webpack_require__(1);

var _index2 = __webpack_require__(16);

var _index3 = __webpack_require__(10);

var _focus = __webpack_require__(145);

function nextOrClose(tooltip, event) {
  var target = event.target,
      inputs = (0, _index2.all)('input', tooltip.element),
      next = inputs[(0, _index3.indexOf)(inputs, target) + 1];

  (0, _index.prevent)(event);

  if (next) {
    return (0, _focus.focus)(next);
  }

  tooltip.close();
}

/***/ }),
/* 56 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onBlur = onBlur;

var _ = __webpack_require__(8);

function onBlur(element, listener, useCapture) {
  return (0, _.on)('blur', element, listener, useCapture);
}

/***/ }),
/* 57 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.cut = cut;

var _contains = __webpack_require__(87);

var _isElement = __webpack_require__(29);

var _isEmpty = __webpack_require__(25);

var _isText = __webpack_require__(24);

var _fragment = __webpack_require__(62);

var _closestOrSelf = __webpack_require__(35);

var _firstNode = __webpack_require__(37);

var _nextNode = __webpack_require__(49);

var _nextNodes = __webpack_require__(154);

var _nodes = __webpack_require__(17);

var _parent = __webpack_require__(3);

var _append = __webpack_require__(19);

var _appendAll = __webpack_require__(28);

var _clone = __webpack_require__(68);

var _text = __webpack_require__(12);

function cut(node, offset) {
  return recurse((0, _closestOrSelf.closestOrSelf)('[contenteditable=true]', node), node, offset);
}

function recurse(parent, target, offset) {
  var childNodes = void 0,
      container = void 0,
      current = void 0,
      next = void 0,
      siblings = void 0;

  if (!parent) {
    return (0, _fragment.fragment)();
  }

  childNodes = (0, _nodes.nodes)(parent);

  if (!childNodes.length) {
    return (0, _fragment.fragment)();
  }

  container = (0, _clone.clone)(parent);

  if (parent === target) {
    parent = (0, _isElement.isElement)(parent) ? parent : (0, _parent.parent)(target);
    current = childNodes[offset];
    siblings = (0, _nextNodes.nextNodes)(current);
    (0, _append.append)(current, container);
    (0, _appendAll.appendAll)(siblings, container);

    return container;
  }

  current = (0, _firstNode.firstNode)(parent);

  while (current && !(0, _contains.contains)(container, current)) {
    next = (0, _nextNode.nextNode)(current);

    if (!(0, _isEmpty.isEmpty)(current)) {
      siblings = (0, _nextNodes.nextNodes)(current);

      if ((0, _isText.isText)(current) && current === target) {
        (0, _append.append)(extractText(current, offset), container);
        (0, _appendAll.appendAll)(siblings, container);
      } else if ((0, _isElement.isElement)(current) && (0, _contains.contains)(current, target)) {
        (0, _append.append)(recurse(current, target, offset), container);
        (0, _appendAll.appendAll)(siblings, container);
      }
    }

    current = next;
  }

  return container;
}

function extractText(node, offset) {
  var container = (0, _clone.clone)(node, true);

  (0, _text.text)(container, (0, _text.text)(container).substr(offset));
  (0, _text.text)(node, (0, _text.text)(node).substr(0, offset));

  return container;
}

/***/ }),
/* 58 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.end = end;

var _lastOrSelf = __webpack_require__(156);

var _text = __webpack_require__(12);

var _select = __webpack_require__(74);

function end(node) {
  var last = (0, _lastOrSelf.lastOrSelf)(node);

  (0, _select.select)(last, (0, _text.text)(last).length);

  return last;
}

/***/ }),
/* 59 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.listenLi = listenLi;

var _onBack = __webpack_require__(51);

var _onBlur = __webpack_require__(56);

var _onDelete = __webpack_require__(83);

var _onEnter = __webpack_require__(26);

var _onLeft = __webpack_require__(84);

var _onRight = __webpack_require__(85);

var _onBlurEvent = __webpack_require__(181);

var _onEnterEvent = __webpack_require__(182);

var _onBackEvent = __webpack_require__(184);

var _onDeleteEvent = __webpack_require__(185);

var _onLeftEvent = __webpack_require__(186);

var _onRightEvent = __webpack_require__(187);

function listenLi(element) {
  (0, _onBlur.onBlur)(element, _onBlurEvent.onBlurEvent, true);
  (0, _onEnter.onEnter)(element, _onEnterEvent.onEnterEvent, true);
  (0, _onBack.onBack)(element, _onBackEvent.onBackEvent, true);
  (0, _onDelete.onDelete)(element, _onDeleteEvent.onDeleteEvent, true);
  (0, _onLeft.onLeft)(element, _onLeftEvent.onLeftEvent, true);
  (0, _onRight.onRight)(element, _onRightEvent.onRightEvent, true);

  return element;
}

/***/ }),
/* 60 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getTarget = getTarget;
function getTarget(event) {
  return event.target;
}

/***/ }),
/* 61 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
var keys = exports.keys = Object.keys;

/***/ }),
/* 62 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.fragment = fragment;

var _document = __webpack_require__(20);

var _isCollection = __webpack_require__(98);

var _wrapAll = __webpack_require__(99);

var _wrap = __webpack_require__(63);

function fragment(contents) {
  var node = (0, _document.document)().createDocumentFragment();

  if (!contents) {
    return node;
  }

  if ((0, _isCollection.isCollection)(contents)) {
    return (0, _wrapAll.wrapAll)(contents, node);
  }

  return (0, _wrap.wrap)(contents);
}

/***/ }),
/* 63 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.wrap = wrap;

var _before = __webpack_require__(15);

function wrap(node, wrapper) {
  (0, _before.before)(node, null, wrapper);

  return wrapper;
}

/***/ }),
/* 64 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isUndefined = isUndefined;
function isUndefined(value) {
  return value === undefined;
}

/***/ }),
/* 65 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.callEach = callEach;

var _create = __webpack_require__(14);

var _global = __webpack_require__(9);

var _call = __webpack_require__(100);

var events = (0, _create.create)(),
    listen = (0, _global.global)().Element.prototype.addEventListener;

events.blur = ['blur', 'touchcancel', 'touchleave'];
events.blur.listener = function (listener, event) {
  return listener.call(this, event);
};

events.click = ['click', 'touchend'];
events.click.listener = function (listener, event) {
  if (!event.touches || event.touches.length === 1) {
    return listener.call(this, event);
  }
};

events.focus = ['focus', 'touchstart'];
events.focus.listener = events.blur.listener;

function callEach(method, event, element, listener, useCapture) {
  var realListener = listener,
      names = void 0,
      key = 0,
      length = void 0;

  if (event in events) {
    names = events[event];
    length = names.length;

    if (method === listen) {
      realListener = names.listener.bind(element, realListener);
    }

    for (key; key < length; key += 1) {
      (0, _call.call)(method, names[key], element, realListener, useCapture);
    }

    return realListener;
  }

  (0, _call.call)(method, event, element, realListener, useCapture);

  return realListener;
}

/***/ }),
/* 66 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onSubmit = onSubmit;

var _ = __webpack_require__(8);

function onSubmit(element, listener, useCapture) {
  return (0, _.on)('submit', element, listener, useCapture);
}

/***/ }),
/* 67 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
var queue = exports.queue = [];

queue.next = function () {
  queue.shift();

  if (queue.length) {
    queue[0].fetchRequest();
  }
};

/***/ }),
/* 68 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.clone = clone;
function clone(node, deep) {
  return node.cloneNode(deep);
}

/***/ }),
/* 69 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isBoolean = isBoolean;
function isBoolean(value) {
  return typeof value === 'boolean';
}

/***/ }),
/* 70 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.camelToDash = camelToDash;
function format(value) {
  return '-'.concat(value.toLowerCase());
}

function camelToDash(value) {
  return value.replace(/([A-Z])/g, format);
}

/***/ }),
/* 71 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.dashToCamel = dashToCamel;
function format(value) {
  return value.toUpperCase().replace('-', '');
}

function dashToCamel(value) {
  return value.replace(/(-[a-z])/g, format);
}

/***/ }),
/* 72 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.listenH1 = listenH1;

var _onBack = __webpack_require__(51);

var _onEnter = __webpack_require__(26);

var _onShiftEnter = __webpack_require__(73);

var _prevent = __webpack_require__(1);

var _closest = __webpack_require__(11);

var _editables = __webpack_require__(27);

var _isEmpty = __webpack_require__(25);

var _next = __webpack_require__(52);

var _clean = __webpack_require__(7);

var _remove = __webpack_require__(5);

function listenH1(element) {
  (0, _onEnter.onEnter)(element, onEnterEvent, true);
  (0, _onShiftEnter.onShiftEnter)(element, _prevent.prevent, true);
  (0, _onBack.onBack)(element, removeSection, true);
}

function onEnterEvent(event) {
  var target = event.target;

  (0, _prevent.prevent)(event);
  (0, _next.next)(target, (0, _editables.editables)(target));
}

function removeSection(event) {
  var target = event.target,
      section = (0, _closest.closest)('section', target);

  (0, _clean.clean)(target);

  if (section && (0, _isEmpty.isEmpty)(target)) {
    (0, _remove.remove)(section);
  }
}

/***/ }),
/* 73 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onShiftEnter = onShiftEnter;

var _ = __webpack_require__(8);

function onShiftEnter(element, listener, useCapture) {
  return (0, _.on)('keydown', element, function (event) {
    if (event.which === 13 && event.shiftKey && !event.ctrlKey) {
      return listener.call(this, event);
    }
  }, useCapture);
}

/***/ }),
/* 74 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.select = select;

var _current = __webpack_require__(4);

var _document = __webpack_require__(20);

function select(node, begin, end) {
  var range = (0, _document.document)().createRange(),
      selection = (0, _current.current)();

  range.selectNodeContents(node);

  if (Number.isInteger(begin)) {
    range.setStart(node, begin);
  }

  if (Number.isInteger(end)) {
    range.setEnd(node, end);
  }

  selection.removeAllRanges();
  selection.addRange(range);

  if (!Number.isInteger(begin)) {
    selection.collapseToEnd();
  }

  if (!Number.isInteger(end)) {
    selection.collapseToStart();
  }

  return node;
}

/***/ }),
/* 75 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.fromInput = fromInput;

var _onFileInput = __webpack_require__(138);

var _curry = __webpack_require__(13);

var _index = __webpack_require__(76);

function fromInput(callback, input) {
  (0, _onFileInput.onFileInput)(input, (0, _curry.curry)(read, callback));
}

function read(callback, event) {
  var file = (event.dataTransfer || event.target).files[0];

  callback((0, _index.blobToImage)(file));
}

/***/ }),
/* 76 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.blobToImage = blobToImage;

var _global = __webpack_require__(9);

var _index = __webpack_require__(141);

var _index2 = __webpack_require__(78);

var window = (0, _global.global)(),
    revokeObjectURL = window.URL.revokeObjectURL;

function blobToImage(blob) {
  return (0, _index2.blobToDataURL)(blob).then(_index.dataURLToImage).then(revoke);
}

function revoke(target) {
  revokeObjectURL(target.src);

  return target;
}

/***/ }),
/* 77 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.once = once;

var _on = __webpack_require__(8);

var _off = __webpack_require__(143);

/**
 * Listens once an event on an element (touch or not)
 * @param {String} event
 * @param {Element} element
 * @param {Function} listener
 * @param {Boolean} useCapture
 * @returns {Function} listener
 */
function once(event, element, listener, useCapture) {
  var realListener = (0, _on.on)(event, element, function (event) {
    (0, _off.off)(event.type, element, realListener, useCapture);

    return listener.call(this, event);
  }, useCapture);

  return realListener;
}

/***/ }),
/* 78 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.blobToDataURL = blobToDataURL;

var _global = __webpack_require__(9);

var _promise = __webpack_require__(38);

var window = (0, _global.global)(),
    createObjectURL = window.URL.createObjectURL;

function blobToDataURL(blob) {
  return (0, _promise.promise)(read, blob);
}

function read(blob, resolve) {
  resolve(createObjectURL(blob));
}

/***/ }),
/* 79 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onFile = onFile;

var _curry = __webpack_require__(13);

var _compress = __webpack_require__(146);

var _resize = __webpack_require__(147);

var _imageToDataURL = __webpack_require__(148);

var _display = __webpack_require__(150);

function onFile(tooltip, options, promise) {
  return promise.then((0, _curry.curry)(_resize.resize, options)).then((0, _curry.curry)(_compress.compress, options)).then((0, _curry.curry)(_imageToDataURL.imageToDataURL, options)).then((0, _curry.curry)(_display.display, tooltip));
}

/***/ }),
/* 80 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.canvasToImage = canvasToImage;

var _blobToImage = __webpack_require__(76);

var _index = __webpack_require__(81);

function canvasToImage(options, canvas) {
  return (0, _index.canvasToBlob)(options, canvas).then(_blobToImage.blobToImage);
}

/***/ }),
/* 81 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.canvasToBlob = canvasToBlob;

var _create = __webpack_require__(14);

var _curry = __webpack_require__(13);

var _promise = __webpack_require__(38);

function canvasToBlob(options, canvas) {
  var promised = (0, _promise.promise)(toBlob, options, canvas);

  if (!options.maxSize) {
    return promised;
  }

  return promised.then((0, _curry.curry)(read, options, canvas));
}

function toBlob(options, canvas, resolve) {
  canvas.toBlob(resolve, options.mime, options.quality);
}

function read(options, canvas, blob) {
  var size = blob.size;

  if (size < options.maxSize) {
    return blob;
  }

  options = (0, _create.create)(options);
  options.quality = options.quality / (size / options.maxSize);

  return canvasToBlob(options, canvas);
}

/***/ }),
/* 82 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.imageToCanvas = imageToCanvas;

var _element = __webpack_require__(22);

var _update = __webpack_require__(36);

var _index = __webpack_require__(38);

function imageToCanvas(img) {
  return (0, _index.promise)(toCanvas, img);
}

function toCanvas(img, resolve) {
  var canvas = (0, _update.update)((0, _element.element)('canvas'), {
    width: img.naturalWidth,
    height: img.naturalHeight
  }),
      context = canvas.getContext('2d');

  context.fillStyle = 'transparent';
  context.fillRect(0, 0, img.naturalWidth, img.naturalHeight);
  context.save();
  context.drawImage(img, 0, 0);
  resolve(canvas);
}

/***/ }),
/* 83 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onDelete = onDelete;

var _ = __webpack_require__(8);

function onDelete(element, listener, useCapture) {
  return (0, _.on)('keydown', element, function (event) {
    if (event.which === 46) {
      return listener.call(this, event);
    }
  }, useCapture);
}

/***/ }),
/* 84 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onLeft = onLeft;

var _ = __webpack_require__(8);

function onLeft(element, listener, useCapture) {
  return (0, _.on)('keydown', element, function (event) {
    if (event.which === 37) {
      return listener.call(this, event);
    }
  }, useCapture);
}

/***/ }),
/* 85 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onRight = onRight;

var _ = __webpack_require__(8);

function onRight(element, listener, useCapture) {
  return (0, _.on)('keydown', element, function (event) {
    if (event.which === 39) {
      return listener.call(this, event);
    }
  }, useCapture);
}

/***/ }),
/* 86 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.firstElement = firstElement;
function firstElement(node) {
  return node.firstElementChild;
}

/***/ }),
/* 87 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.contains = contains;
function contains(value, node) {
  return node.contains(value);
}

/***/ }),
/* 88 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.lastNode = lastNode;
function lastNode(node) {
  return node.lastChild;
}

/***/ }),
/* 89 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isCollapsedToNext = isCollapsedToNext;

var _current = __webpack_require__(4);

var _hasSameNodeName = __webpack_require__(158);

var _text = __webpack_require__(12);

var _nextElement = __webpack_require__(40);

var _lastNode = __webpack_require__(88);

function isCollapsedToNext(event) {
  var target = event.target,
      selection = (0, _current.current)(),
      focused = selection.focusNode,
      last = (0, _lastNode.lastNode)(target),
      next = (0, _nextElement.nextElement)(target);

  return next && selection.isCollapsed && (0, _hasSameNodeName.hasSameNodeName)(target, next) && selection.focusOffset === (0, _text.text)(last).length && last === focused;
}

/***/ }),
/* 90 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isFirstLi = isFirstLi;

var _isText = __webpack_require__(24);

var _matches = __webpack_require__(23);

var _closest = __webpack_require__(11);

var _nodeName = __webpack_require__(18);

var _parent = __webpack_require__(3);

var _firstElement = __webpack_require__(86);

function isFirstLi(node) {
  if (!node) {
    return false;
  }

  if ((0, _isText.isText)(node)) {
    node = (0, _closest.closest)('li', node);
  }

  return (0, _nodeName.nodeName)(node) === 'li' && (0, _firstElement.firstElement)((0, _parent.parent)(node)) === node;
}

/***/ }),
/* 91 */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(92);
__webpack_require__(93);
__webpack_require__(96);
__webpack_require__(0);
module.exports = __webpack_require__(103);


/***/ }),
/* 92 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

// URL Polyfill
// Draft specification: https://url.spec.whatwg.org

// Notes:
// - Primarily useful for parsing URLs and modifying query parameters
// - Should work in IE8+ and everything more modern, with es5.js polyfills

(function (global) {
  'use strict';

  function isSequence(o) {
    if (!o) return false;
    if ('Symbol' in global && 'iterator' in global.Symbol && typeof o[Symbol.iterator] === 'function') return true;
    if (Array.isArray(o)) return true;
    return false;
  }

  function toArray(iter) {
    return 'from' in Array ? Array.from(iter) : Array.prototype.slice.call(iter);
  }

  (function () {

    // Browsers may have:
    // * No global URL object
    // * URL with static methods only - may have a dummy constructor
    // * URL with members except searchParams
    // * Full URL API support
    var origURL = global.URL;
    var nativeURL;
    try {
      if (origURL) {
        nativeURL = new global.URL('http://example.com');
        if ('searchParams' in nativeURL) return;
        if (!('href' in nativeURL)) nativeURL = undefined;
      }
    } catch (_) {}

    // NOTE: Doesn't do the encoding/decoding dance
    function urlencoded_serialize(pairs) {
      var output = '',
          first = true;
      pairs.forEach(function (pair) {
        var name = encodeURIComponent(pair.name);
        var value = encodeURIComponent(pair.value);
        if (!first) output += '&';
        output += name + '=' + value;
        first = false;
      });
      return output.replace(/%20/g, '+');
    }

    // NOTE: Doesn't do the encoding/decoding dance
    function urlencoded_parse(input, isindex) {
      var sequences = input.split('&');
      if (isindex && sequences[0].indexOf('=') === -1) sequences[0] = '=' + sequences[0];
      var pairs = [];
      sequences.forEach(function (bytes) {
        if (bytes.length === 0) return;
        var index = bytes.indexOf('=');
        if (index !== -1) {
          var name = bytes.substring(0, index);
          var value = bytes.substring(index + 1);
        } else {
          name = bytes;
          value = '';
        }
        name = name.replace(/\+/g, ' ');
        value = value.replace(/\+/g, ' ');
        pairs.push({ name: name, value: value });
      });
      var output = [];
      pairs.forEach(function (pair) {
        output.push({
          name: decodeURIComponent(pair.name),
          value: decodeURIComponent(pair.value)
        });
      });
      return output;
    }

    function URLUtils(url) {
      if (nativeURL) return new origURL(url);
      var anchor = document.createElement('a');
      anchor.href = url;
      return anchor;
    }

    function URLSearchParams(init) {
      var $this = this;
      this._list = [];

      if (init === undefined || init === null) {
        // no-op
      } else if (init instanceof URLSearchParams) {
        // In ES6 init would be a sequence, but special case for ES5.
        this._list = urlencoded_parse(String(init));
      } else if ((typeof init === 'undefined' ? 'undefined' : _typeof(init)) === 'object' && isSequence(init)) {
        toArray(init).forEach(function (e) {
          if (!isSequence(e)) throw TypeError();
          var nv = toArray(e);
          if (nv.length !== 2) throw TypeError();
          $this._list.push({ name: String(nv[0]), value: String(nv[1]) });
        });
      } else if ((typeof init === 'undefined' ? 'undefined' : _typeof(init)) === 'object' && init) {
        Object.keys(init).forEach(function (key) {
          $this._list.push({ name: String(key), value: String(init[key]) });
        });
      } else {
        init = String(init);
        if (init.substring(0, 1) === '?') init = init.substring(1);
        this._list = urlencoded_parse(init);
      }

      this._url_object = null;
      this._setList = function (list) {
        if (!updating) $this._list = list;
      };

      var updating = false;
      this._update_steps = function () {
        if (updating) return;
        updating = true;

        if (!$this._url_object) return;

        // Partial workaround for IE issue with 'about:'
        if ($this._url_object.protocol === 'about:' && $this._url_object.pathname.indexOf('?') !== -1) {
          $this._url_object.pathname = $this._url_object.pathname.split('?')[0];
        }

        $this._url_object.search = urlencoded_serialize($this._list);

        updating = false;
      };
    }

    Object.defineProperties(URLSearchParams.prototype, {
      append: {
        value: function value(name, _value) {
          this._list.push({ name: name, value: _value });
          this._update_steps();
        }, writable: true, enumerable: true, configurable: true
      },

      'delete': {
        value: function value(name) {
          for (var i = 0; i < this._list.length;) {
            if (this._list[i].name === name) this._list.splice(i, 1);else ++i;
          }
          this._update_steps();
        }, writable: true, enumerable: true, configurable: true
      },

      get: {
        value: function value(name) {
          for (var i = 0; i < this._list.length; ++i) {
            if (this._list[i].name === name) return this._list[i].value;
          }
          return null;
        }, writable: true, enumerable: true, configurable: true
      },

      getAll: {
        value: function value(name) {
          var result = [];
          for (var i = 0; i < this._list.length; ++i) {
            if (this._list[i].name === name) result.push(this._list[i].value);
          }
          return result;
        }, writable: true, enumerable: true, configurable: true
      },

      has: {
        value: function value(name) {
          for (var i = 0; i < this._list.length; ++i) {
            if (this._list[i].name === name) return true;
          }
          return false;
        }, writable: true, enumerable: true, configurable: true
      },

      set: {
        value: function value(name, _value2) {
          var found = false;
          for (var i = 0; i < this._list.length;) {
            if (this._list[i].name === name) {
              if (!found) {
                this._list[i].value = _value2;
                found = true;
                ++i;
              } else {
                this._list.splice(i, 1);
              }
            } else {
              ++i;
            }
          }

          if (!found) this._list.push({ name: name, value: _value2 });

          this._update_steps();
        }, writable: true, enumerable: true, configurable: true
      },

      entries: {
        value: function value() {
          return new Iterator(this._list, 'key+value');
        },
        writable: true, enumerable: true, configurable: true
      },

      keys: {
        value: function value() {
          return new Iterator(this._list, 'key');
        },
        writable: true, enumerable: true, configurable: true
      },

      values: {
        value: function value() {
          return new Iterator(this._list, 'value');
        },
        writable: true, enumerable: true, configurable: true
      },

      forEach: {
        value: function value(callback) {
          var thisArg = arguments.length > 1 ? arguments[1] : undefined;
          this._list.forEach(function (pair, index) {
            callback.call(thisArg, pair.value, pair.name);
          });
        }, writable: true, enumerable: true, configurable: true
      },

      toString: {
        value: function value() {
          return urlencoded_serialize(this._list);
        }, writable: true, enumerable: false, configurable: true
      }
    });

    function Iterator(source, kind) {
      var index = 0;
      this['next'] = function () {
        if (index >= source.length) return { done: true, value: undefined };
        var pair = source[index++];
        return { done: false, value: kind === 'key' ? pair.name : kind === 'value' ? pair.value : [pair.name, pair.value] };
      };
    }

    if ('Symbol' in global && 'iterator' in global.Symbol) {
      Object.defineProperty(URLSearchParams.prototype, global.Symbol.iterator, {
        value: URLSearchParams.prototype.entries,
        writable: true, enumerable: true, configurable: true });
      Object.defineProperty(Iterator.prototype, global.Symbol.iterator, {
        value: function value() {
          return this;
        },
        writable: true, enumerable: true, configurable: true });
    }

    function URL(url, base) {
      if (!(this instanceof global.URL)) throw new TypeError("Failed to construct 'URL': Please use the 'new' operator.");

      if (base) {
        url = function () {
          if (nativeURL) return new origURL(url, base).href;
          var iframe;
          try {
            var doc;
            // Use another document/base tag/anchor for relative URL resolution, if possible
            if (Object.prototype.toString.call(window.operamini) === "[object OperaMini]") {
              iframe = document.createElement('iframe');
              iframe.style.display = 'none';
              document.documentElement.appendChild(iframe);
              doc = iframe.contentWindow.document;
            } else if (document.implementation && document.implementation.createHTMLDocument) {
              doc = document.implementation.createHTMLDocument('');
            } else if (document.implementation && document.implementation.createDocument) {
              doc = document.implementation.createDocument('http://www.w3.org/1999/xhtml', 'html', null);
              doc.documentElement.appendChild(doc.createElement('head'));
              doc.documentElement.appendChild(doc.createElement('body'));
            } else if (window.ActiveXObject) {
              doc = new window.ActiveXObject('htmlfile');
              doc.write('<head><\/head><body><\/body>');
              doc.close();
            }

            if (!doc) throw Error('base not supported');

            var baseTag = doc.createElement('base');
            baseTag.href = base;
            doc.getElementsByTagName('head')[0].appendChild(baseTag);
            var anchor = doc.createElement('a');
            anchor.href = url;
            return anchor.href;
          } finally {
            if (iframe) iframe.parentNode.removeChild(iframe);
          }
        }();
      }

      // An inner object implementing URLUtils (either a native URL
      // object or an HTMLAnchorElement instance) is used to perform the
      // URL algorithms. With full ES5 getter/setter support, return a
      // regular object For IE8's limited getter/setter support, a
      // different HTMLAnchorElement is returned with properties
      // overridden

      var instance = URLUtils(url || '');

      // Detect for ES5 getter/setter support
      // (an Object.defineProperties polyfill that doesn't support getters/setters may throw)
      var ES5_GET_SET = function () {
        if (!('defineProperties' in Object)) return false;
        try {
          var obj = {};
          Object.defineProperties(obj, { prop: { 'get': function get() {
                return true;
              } } });
          return obj.prop;
        } catch (_) {
          return false;
        }
      }();

      var self = ES5_GET_SET ? this : document.createElement('a');

      var query_object = new URLSearchParams(instance.search ? instance.search.substring(1) : null);
      query_object._url_object = self;

      Object.defineProperties(self, {
        href: {
          get: function get() {
            return instance.href;
          },
          set: function set(v) {
            instance.href = v;tidy_instance();update_steps();
          },
          enumerable: true, configurable: true
        },
        origin: {
          get: function get() {
            if ('origin' in instance) return instance.origin;
            return this.protocol + '//' + this.host;
          },
          enumerable: true, configurable: true
        },
        protocol: {
          get: function get() {
            return instance.protocol;
          },
          set: function set(v) {
            instance.protocol = v;
          },
          enumerable: true, configurable: true
        },
        username: {
          get: function get() {
            return instance.username;
          },
          set: function set(v) {
            instance.username = v;
          },
          enumerable: true, configurable: true
        },
        password: {
          get: function get() {
            return instance.password;
          },
          set: function set(v) {
            instance.password = v;
          },
          enumerable: true, configurable: true
        },
        host: {
          get: function get() {
            // IE returns default port in |host|
            var re = { 'http:': /:80$/, 'https:': /:443$/, 'ftp:': /:21$/ }[instance.protocol];
            return re ? instance.host.replace(re, '') : instance.host;
          },
          set: function set(v) {
            instance.host = v;
          },
          enumerable: true, configurable: true
        },
        hostname: {
          get: function get() {
            return instance.hostname;
          },
          set: function set(v) {
            instance.hostname = v;
          },
          enumerable: true, configurable: true
        },
        port: {
          get: function get() {
            return instance.port;
          },
          set: function set(v) {
            instance.port = v;
          },
          enumerable: true, configurable: true
        },
        pathname: {
          get: function get() {
            // IE does not include leading '/' in |pathname|
            if (instance.pathname.charAt(0) !== '/') return '/' + instance.pathname;
            return instance.pathname;
          },
          set: function set(v) {
            instance.pathname = v;
          },
          enumerable: true, configurable: true
        },
        search: {
          get: function get() {
            return instance.search;
          },
          set: function set(v) {
            if (instance.search === v) return;
            instance.search = v;tidy_instance();update_steps();
          },
          enumerable: true, configurable: true
        },
        searchParams: {
          get: function get() {
            return query_object;
          },
          enumerable: true, configurable: true
        },
        hash: {
          get: function get() {
            return instance.hash;
          },
          set: function set(v) {
            instance.hash = v;tidy_instance();
          },
          enumerable: true, configurable: true
        },
        toString: {
          value: function value() {
            return instance.toString();
          },
          enumerable: false, configurable: true
        },
        valueOf: {
          value: function value() {
            return instance.valueOf();
          },
          enumerable: false, configurable: true
        }
      });

      function tidy_instance() {
        var href = instance.href.replace(/#$|\?$|\?(?=#)/g, '');
        if (instance.href !== href) instance.href = href;
      }

      function update_steps() {
        query_object._setList(instance.search ? urlencoded_parse(instance.search.substring(1)) : []);
        query_object._update_steps();
      };

      return self;
    }

    if (origURL) {
      for (var i in origURL) {
        if (origURL.hasOwnProperty(i) && typeof origURL[i] === 'function') URL[i] = origURL[i];
      }
    }

    global.URL = URL;
    global.URLSearchParams = URLSearchParams;
  })();

  // Patch native URLSearchParams constructor to handle sequences/records
  // if necessary.
  (function () {
    if (new global.URLSearchParams([['a', 1]]).get('a') === '1' && new global.URLSearchParams({ a: 1 }).get('a') === '1') return;
    var orig = global.URLSearchParams;
    global.URLSearchParams = function (init) {
      if (init && (typeof init === 'undefined' ? 'undefined' : _typeof(init)) === 'object' && isSequence(init)) {
        var o = new orig();
        toArray(init).forEach(function (e) {
          if (!isSequence(e)) throw TypeError();
          var nv = toArray(e);
          if (nv.length !== 2) throw TypeError();
          o.append(nv[0], nv[1]);
        });
        return o;
      } else if (init && (typeof init === 'undefined' ? 'undefined' : _typeof(init)) === 'object') {
        o = new orig();
        Object.keys(init).forEach(function (key) {
          o.set(key, init[key]);
        });
        return o;
      } else {
        return new orig(init);
      }
    };
  })();
})(self);

/***/ }),
/* 93 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process, global) {var __WEBPACK_AMD_DEFINE_FACTORY__, __WEBPACK_AMD_DEFINE_RESULT__;

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

/*!
 * @overview es6-promise - a tiny implementation of Promises/A+.
 * @copyright Copyright (c) 2014 Yehuda Katz, Tom Dale, Stefan Penner and contributors (Conversion to ES6 API by Jake Archibald)
 * @license   Licensed under MIT license
 *            See https://raw.githubusercontent.com/stefanpenner/es6-promise/master/LICENSE
 * @version   v4.2.4+314e4831
 */

(function (global, factory) {
  ( false ? 'undefined' : _typeof(exports)) === 'object' && typeof module !== 'undefined' ? module.exports = factory() :  true ? !(__WEBPACK_AMD_DEFINE_FACTORY__ = (factory),
				__WEBPACK_AMD_DEFINE_RESULT__ = (typeof __WEBPACK_AMD_DEFINE_FACTORY__ === 'function' ?
				(__WEBPACK_AMD_DEFINE_FACTORY__.call(exports, __webpack_require__, exports, module)) :
				__WEBPACK_AMD_DEFINE_FACTORY__),
				__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__)) : global.ES6Promise = factory();
})(undefined, function () {
  'use strict';

  function objectOrFunction(x) {
    var type = typeof x === 'undefined' ? 'undefined' : _typeof(x);
    return x !== null && (type === 'object' || type === 'function');
  }

  function isFunction(x) {
    return typeof x === 'function';
  }

  var _isArray = void 0;
  if (Array.isArray) {
    _isArray = Array.isArray;
  } else {
    _isArray = function _isArray(x) {
      return Object.prototype.toString.call(x) === '[object Array]';
    };
  }

  var isArray = _isArray;

  var len = 0;
  var vertxNext = void 0;
  var customSchedulerFn = void 0;

  var asap = function asap(callback, arg) {
    queue[len] = callback;
    queue[len + 1] = arg;
    len += 2;
    if (len === 2) {
      // If len is 2, that means that we need to schedule an async flush.
      // If additional callbacks are queued before the queue is flushed, they
      // will be processed by this flush that we are scheduling.
      if (customSchedulerFn) {
        customSchedulerFn(flush);
      } else {
        scheduleFlush();
      }
    }
  };

  function setScheduler(scheduleFn) {
    customSchedulerFn = scheduleFn;
  }

  function setAsap(asapFn) {
    asap = asapFn;
  }

  var browserWindow = typeof window !== 'undefined' ? window : undefined;
  var browserGlobal = browserWindow || {};
  var BrowserMutationObserver = browserGlobal.MutationObserver || browserGlobal.WebKitMutationObserver;
  var isNode = typeof self === 'undefined' && typeof process !== 'undefined' && {}.toString.call(process) === '[object process]';

  // test for web worker but not in IE10
  var isWorker = typeof Uint8ClampedArray !== 'undefined' && typeof importScripts !== 'undefined' && typeof MessageChannel !== 'undefined';

  // node
  function useNextTick() {
    // node version 0.10.x displays a deprecation warning when nextTick is used recursively
    // see https://github.com/cujojs/when/issues/410 for details
    return function () {
      return process.nextTick(flush);
    };
  }

  // vertx
  function useVertxTimer() {
    if (typeof vertxNext !== 'undefined') {
      return function () {
        vertxNext(flush);
      };
    }

    return useSetTimeout();
  }

  function useMutationObserver() {
    var iterations = 0;
    var observer = new BrowserMutationObserver(flush);
    var node = document.createTextNode('');
    observer.observe(node, { characterData: true });

    return function () {
      node.data = iterations = ++iterations % 2;
    };
  }

  // web worker
  function useMessageChannel() {
    var channel = new MessageChannel();
    channel.port1.onmessage = flush;
    return function () {
      return channel.port2.postMessage(0);
    };
  }

  function useSetTimeout() {
    // Store setTimeout reference so es6-promise will be unaffected by
    // other code modifying setTimeout (like sinon.useFakeTimers())
    var globalSetTimeout = setTimeout;
    return function () {
      return globalSetTimeout(flush, 1);
    };
  }

  var queue = new Array(1000);
  function flush() {
    for (var i = 0; i < len; i += 2) {
      var callback = queue[i];
      var arg = queue[i + 1];

      callback(arg);

      queue[i] = undefined;
      queue[i + 1] = undefined;
    }

    len = 0;
  }

  function attemptVertx() {
    try {
      var vertx = Function('return this')().require('vertx');
      vertxNext = vertx.runOnLoop || vertx.runOnContext;
      return useVertxTimer();
    } catch (e) {
      return useSetTimeout();
    }
  }

  var scheduleFlush = void 0;
  // Decide what async method to use to triggering processing of queued callbacks:
  if (isNode) {
    scheduleFlush = useNextTick();
  } else if (BrowserMutationObserver) {
    scheduleFlush = useMutationObserver();
  } else if (isWorker) {
    scheduleFlush = useMessageChannel();
  } else if (browserWindow === undefined && "function" === 'function') {
    scheduleFlush = attemptVertx();
  } else {
    scheduleFlush = useSetTimeout();
  }

  function then(onFulfillment, onRejection) {
    var parent = this;

    var child = new this.constructor(noop);

    if (child[PROMISE_ID] === undefined) {
      makePromise(child);
    }

    var _state = parent._state;

    if (_state) {
      var callback = arguments[_state - 1];
      asap(function () {
        return invokeCallback(_state, child, callback, parent._result);
      });
    } else {
      subscribe(parent, child, onFulfillment, onRejection);
    }

    return child;
  }

  /**
    `Promise.resolve` returns a promise that will become resolved with the
    passed `value`. It is shorthand for the following:
  
    ```javascript
    let promise = new Promise(function(resolve, reject){
      resolve(1);
    });
  
    promise.then(function(value){
      // value === 1
    });
    ```
  
    Instead of writing the above, your code now simply becomes the following:
  
    ```javascript
    let promise = Promise.resolve(1);
  
    promise.then(function(value){
      // value === 1
    });
    ```
  
    @method resolve
    @static
    @param {Any} value value that the returned promise will be resolved with
    Useful for tooling.
    @return {Promise} a promise that will become fulfilled with the given
    `value`
  */
  function resolve$1(object) {
    /*jshint validthis:true */
    var Constructor = this;

    if (object && (typeof object === 'undefined' ? 'undefined' : _typeof(object)) === 'object' && object.constructor === Constructor) {
      return object;
    }

    var promise = new Constructor(noop);
    resolve(promise, object);
    return promise;
  }

  var PROMISE_ID = Math.random().toString(36).substring(2);

  function noop() {}

  var PENDING = void 0;
  var FULFILLED = 1;
  var REJECTED = 2;

  var TRY_CATCH_ERROR = { error: null };

  function selfFulfillment() {
    return new TypeError("You cannot resolve a promise with itself");
  }

  function cannotReturnOwn() {
    return new TypeError('A promises callback cannot return that same promise.');
  }

  function getThen(promise) {
    try {
      return promise.then;
    } catch (error) {
      TRY_CATCH_ERROR.error = error;
      return TRY_CATCH_ERROR;
    }
  }

  function tryThen(then$$1, value, fulfillmentHandler, rejectionHandler) {
    try {
      then$$1.call(value, fulfillmentHandler, rejectionHandler);
    } catch (e) {
      return e;
    }
  }

  function handleForeignThenable(promise, thenable, then$$1) {
    asap(function (promise) {
      var sealed = false;
      var error = tryThen(then$$1, thenable, function (value) {
        if (sealed) {
          return;
        }
        sealed = true;
        if (thenable !== value) {
          resolve(promise, value);
        } else {
          fulfill(promise, value);
        }
      }, function (reason) {
        if (sealed) {
          return;
        }
        sealed = true;

        reject(promise, reason);
      }, 'Settle: ' + (promise._label || ' unknown promise'));

      if (!sealed && error) {
        sealed = true;
        reject(promise, error);
      }
    }, promise);
  }

  function handleOwnThenable(promise, thenable) {
    if (thenable._state === FULFILLED) {
      fulfill(promise, thenable._result);
    } else if (thenable._state === REJECTED) {
      reject(promise, thenable._result);
    } else {
      subscribe(thenable, undefined, function (value) {
        return resolve(promise, value);
      }, function (reason) {
        return reject(promise, reason);
      });
    }
  }

  function handleMaybeThenable(promise, maybeThenable, then$$1) {
    if (maybeThenable.constructor === promise.constructor && then$$1 === then && maybeThenable.constructor.resolve === resolve$1) {
      handleOwnThenable(promise, maybeThenable);
    } else {
      if (then$$1 === TRY_CATCH_ERROR) {
        reject(promise, TRY_CATCH_ERROR.error);
        TRY_CATCH_ERROR.error = null;
      } else if (then$$1 === undefined) {
        fulfill(promise, maybeThenable);
      } else if (isFunction(then$$1)) {
        handleForeignThenable(promise, maybeThenable, then$$1);
      } else {
        fulfill(promise, maybeThenable);
      }
    }
  }

  function resolve(promise, value) {
    if (promise === value) {
      reject(promise, selfFulfillment());
    } else if (objectOrFunction(value)) {
      handleMaybeThenable(promise, value, getThen(value));
    } else {
      fulfill(promise, value);
    }
  }

  function publishRejection(promise) {
    if (promise._onerror) {
      promise._onerror(promise._result);
    }

    publish(promise);
  }

  function fulfill(promise, value) {
    if (promise._state !== PENDING) {
      return;
    }

    promise._result = value;
    promise._state = FULFILLED;

    if (promise._subscribers.length !== 0) {
      asap(publish, promise);
    }
  }

  function reject(promise, reason) {
    if (promise._state !== PENDING) {
      return;
    }
    promise._state = REJECTED;
    promise._result = reason;

    asap(publishRejection, promise);
  }

  function subscribe(parent, child, onFulfillment, onRejection) {
    var _subscribers = parent._subscribers;
    var length = _subscribers.length;

    parent._onerror = null;

    _subscribers[length] = child;
    _subscribers[length + FULFILLED] = onFulfillment;
    _subscribers[length + REJECTED] = onRejection;

    if (length === 0 && parent._state) {
      asap(publish, parent);
    }
  }

  function publish(promise) {
    var subscribers = promise._subscribers;
    var settled = promise._state;

    if (subscribers.length === 0) {
      return;
    }

    var child = void 0,
        callback = void 0,
        detail = promise._result;

    for (var i = 0; i < subscribers.length; i += 3) {
      child = subscribers[i];
      callback = subscribers[i + settled];

      if (child) {
        invokeCallback(settled, child, callback, detail);
      } else {
        callback(detail);
      }
    }

    promise._subscribers.length = 0;
  }

  function tryCatch(callback, detail) {
    try {
      return callback(detail);
    } catch (e) {
      TRY_CATCH_ERROR.error = e;
      return TRY_CATCH_ERROR;
    }
  }

  function invokeCallback(settled, promise, callback, detail) {
    var hasCallback = isFunction(callback),
        value = void 0,
        error = void 0,
        succeeded = void 0,
        failed = void 0;

    if (hasCallback) {
      value = tryCatch(callback, detail);

      if (value === TRY_CATCH_ERROR) {
        failed = true;
        error = value.error;
        value.error = null;
      } else {
        succeeded = true;
      }

      if (promise === value) {
        reject(promise, cannotReturnOwn());
        return;
      }
    } else {
      value = detail;
      succeeded = true;
    }

    if (promise._state !== PENDING) {
      // noop
    } else if (hasCallback && succeeded) {
      resolve(promise, value);
    } else if (failed) {
      reject(promise, error);
    } else if (settled === FULFILLED) {
      fulfill(promise, value);
    } else if (settled === REJECTED) {
      reject(promise, value);
    }
  }

  function initializePromise(promise, resolver) {
    try {
      resolver(function resolvePromise(value) {
        resolve(promise, value);
      }, function rejectPromise(reason) {
        reject(promise, reason);
      });
    } catch (e) {
      reject(promise, e);
    }
  }

  var id = 0;
  function nextId() {
    return id++;
  }

  function makePromise(promise) {
    promise[PROMISE_ID] = id++;
    promise._state = undefined;
    promise._result = undefined;
    promise._subscribers = [];
  }

  function validationError() {
    return new Error('Array Methods must be provided an Array');
  }

  var Enumerator = function () {
    function Enumerator(Constructor, input) {
      this._instanceConstructor = Constructor;
      this.promise = new Constructor(noop);

      if (!this.promise[PROMISE_ID]) {
        makePromise(this.promise);
      }

      if (isArray(input)) {
        this.length = input.length;
        this._remaining = input.length;

        this._result = new Array(this.length);

        if (this.length === 0) {
          fulfill(this.promise, this._result);
        } else {
          this.length = this.length || 0;
          this._enumerate(input);
          if (this._remaining === 0) {
            fulfill(this.promise, this._result);
          }
        }
      } else {
        reject(this.promise, validationError());
      }
    }

    Enumerator.prototype._enumerate = function _enumerate(input) {
      for (var i = 0; this._state === PENDING && i < input.length; i++) {
        this._eachEntry(input[i], i);
      }
    };

    Enumerator.prototype._eachEntry = function _eachEntry(entry, i) {
      var c = this._instanceConstructor;
      var resolve$$1 = c.resolve;

      if (resolve$$1 === resolve$1) {
        var _then = getThen(entry);

        if (_then === then && entry._state !== PENDING) {
          this._settledAt(entry._state, i, entry._result);
        } else if (typeof _then !== 'function') {
          this._remaining--;
          this._result[i] = entry;
        } else if (c === Promise$2) {
          var promise = new c(noop);
          handleMaybeThenable(promise, entry, _then);
          this._willSettleAt(promise, i);
        } else {
          this._willSettleAt(new c(function (resolve$$1) {
            return resolve$$1(entry);
          }), i);
        }
      } else {
        this._willSettleAt(resolve$$1(entry), i);
      }
    };

    Enumerator.prototype._settledAt = function _settledAt(state, i, value) {
      var promise = this.promise;

      if (promise._state === PENDING) {
        this._remaining--;

        if (state === REJECTED) {
          reject(promise, value);
        } else {
          this._result[i] = value;
        }
      }

      if (this._remaining === 0) {
        fulfill(promise, this._result);
      }
    };

    Enumerator.prototype._willSettleAt = function _willSettleAt(promise, i) {
      var enumerator = this;

      subscribe(promise, undefined, function (value) {
        return enumerator._settledAt(FULFILLED, i, value);
      }, function (reason) {
        return enumerator._settledAt(REJECTED, i, reason);
      });
    };

    return Enumerator;
  }();

  /**
    `Promise.all` accepts an array of promises, and returns a new promise which
    is fulfilled with an array of fulfillment values for the passed promises, or
    rejected with the reason of the first passed promise to be rejected. It casts all
    elements of the passed iterable to promises as it runs this algorithm.
  
    Example:
  
    ```javascript
    let promise1 = resolve(1);
    let promise2 = resolve(2);
    let promise3 = resolve(3);
    let promises = [ promise1, promise2, promise3 ];
  
    Promise.all(promises).then(function(array){
      // The array here would be [ 1, 2, 3 ];
    });
    ```
  
    If any of the `promises` given to `all` are rejected, the first promise
    that is rejected will be given as an argument to the returned promises's
    rejection handler. For example:
  
    Example:
  
    ```javascript
    let promise1 = resolve(1);
    let promise2 = reject(new Error("2"));
    let promise3 = reject(new Error("3"));
    let promises = [ promise1, promise2, promise3 ];
  
    Promise.all(promises).then(function(array){
      // Code here never runs because there are rejected promises!
    }, function(error) {
      // error.message === "2"
    });
    ```
  
    @method all
    @static
    @param {Array} entries array of promises
    @param {String} label optional string for labeling the promise.
    Useful for tooling.
    @return {Promise} promise that is fulfilled when all `promises` have been
    fulfilled, or rejected if any of them become rejected.
    @static
  */
  function all(entries) {
    return new Enumerator(this, entries).promise;
  }

  /**
    `Promise.race` returns a new promise which is settled in the same way as the
    first passed promise to settle.
  
    Example:
  
    ```javascript
    let promise1 = new Promise(function(resolve, reject){
      setTimeout(function(){
        resolve('promise 1');
      }, 200);
    });
  
    let promise2 = new Promise(function(resolve, reject){
      setTimeout(function(){
        resolve('promise 2');
      }, 100);
    });
  
    Promise.race([promise1, promise2]).then(function(result){
      // result === 'promise 2' because it was resolved before promise1
      // was resolved.
    });
    ```
  
    `Promise.race` is deterministic in that only the state of the first
    settled promise matters. For example, even if other promises given to the
    `promises` array argument are resolved, but the first settled promise has
    become rejected before the other promises became fulfilled, the returned
    promise will become rejected:
  
    ```javascript
    let promise1 = new Promise(function(resolve, reject){
      setTimeout(function(){
        resolve('promise 1');
      }, 200);
    });
  
    let promise2 = new Promise(function(resolve, reject){
      setTimeout(function(){
        reject(new Error('promise 2'));
      }, 100);
    });
  
    Promise.race([promise1, promise2]).then(function(result){
      // Code here never runs
    }, function(reason){
      // reason.message === 'promise 2' because promise 2 became rejected before
      // promise 1 became fulfilled
    });
    ```
  
    An example real-world use case is implementing timeouts:
  
    ```javascript
    Promise.race([ajax('foo.json'), timeout(5000)])
    ```
  
    @method race
    @static
    @param {Array} promises array of promises to observe
    Useful for tooling.
    @return {Promise} a promise which settles in the same way as the first passed
    promise to settle.
  */
  function race(entries) {
    /*jshint validthis:true */
    var Constructor = this;

    if (!isArray(entries)) {
      return new Constructor(function (_, reject) {
        return reject(new TypeError('You must pass an array to race.'));
      });
    } else {
      return new Constructor(function (resolve, reject) {
        var length = entries.length;
        for (var i = 0; i < length; i++) {
          Constructor.resolve(entries[i]).then(resolve, reject);
        }
      });
    }
  }

  /**
    `Promise.reject` returns a promise rejected with the passed `reason`.
    It is shorthand for the following:
  
    ```javascript
    let promise = new Promise(function(resolve, reject){
      reject(new Error('WHOOPS'));
    });
  
    promise.then(function(value){
      // Code here doesn't run because the promise is rejected!
    }, function(reason){
      // reason.message === 'WHOOPS'
    });
    ```
  
    Instead of writing the above, your code now simply becomes the following:
  
    ```javascript
    let promise = Promise.reject(new Error('WHOOPS'));
  
    promise.then(function(value){
      // Code here doesn't run because the promise is rejected!
    }, function(reason){
      // reason.message === 'WHOOPS'
    });
    ```
  
    @method reject
    @static
    @param {Any} reason value that the returned promise will be rejected with.
    Useful for tooling.
    @return {Promise} a promise rejected with the given `reason`.
  */
  function reject$1(reason) {
    /*jshint validthis:true */
    var Constructor = this;
    var promise = new Constructor(noop);
    reject(promise, reason);
    return promise;
  }

  function needsResolver() {
    throw new TypeError('You must pass a resolver function as the first argument to the promise constructor');
  }

  function needsNew() {
    throw new TypeError("Failed to construct 'Promise': Please use the 'new' operator, this object constructor cannot be called as a function.");
  }

  /**
    Promise objects represent the eventual result of an asynchronous operation. The
    primary way of interacting with a promise is through its `then` method, which
    registers callbacks to receive either a promise's eventual value or the reason
    why the promise cannot be fulfilled.
  
    Terminology
    -----------
  
    - `promise` is an object or function with a `then` method whose behavior conforms to this specification.
    - `thenable` is an object or function that defines a `then` method.
    - `value` is any legal JavaScript value (including undefined, a thenable, or a promise).
    - `exception` is a value that is thrown using the throw statement.
    - `reason` is a value that indicates why a promise was rejected.
    - `settled` the final resting state of a promise, fulfilled or rejected.
  
    A promise can be in one of three states: pending, fulfilled, or rejected.
  
    Promises that are fulfilled have a fulfillment value and are in the fulfilled
    state.  Promises that are rejected have a rejection reason and are in the
    rejected state.  A fulfillment value is never a thenable.
  
    Promises can also be said to *resolve* a value.  If this value is also a
    promise, then the original promise's settled state will match the value's
    settled state.  So a promise that *resolves* a promise that rejects will
    itself reject, and a promise that *resolves* a promise that fulfills will
    itself fulfill.
  
  
    Basic Usage:
    ------------
  
    ```js
    let promise = new Promise(function(resolve, reject) {
      // on success
      resolve(value);
  
      // on failure
      reject(reason);
    });
  
    promise.then(function(value) {
      // on fulfillment
    }, function(reason) {
      // on rejection
    });
    ```
  
    Advanced Usage:
    ---------------
  
    Promises shine when abstracting away asynchronous interactions such as
    `XMLHttpRequest`s.
  
    ```js
    function getJSON(url) {
      return new Promise(function(resolve, reject){
        let xhr = new XMLHttpRequest();
  
        xhr.open('GET', url);
        xhr.onreadystatechange = handler;
        xhr.responseType = 'json';
        xhr.setRequestHeader('Accept', 'application/json');
        xhr.send();
  
        function handler() {
          if (this.readyState === this.DONE) {
            if (this.status === 200) {
              resolve(this.response);
            } else {
              reject(new Error('getJSON: `' + url + '` failed with status: [' + this.status + ']'));
            }
          }
        };
      });
    }
  
    getJSON('/posts.json').then(function(json) {
      // on fulfillment
    }, function(reason) {
      // on rejection
    });
    ```
  
    Unlike callbacks, promises are great composable primitives.
  
    ```js
    Promise.all([
      getJSON('/posts'),
      getJSON('/comments')
    ]).then(function(values){
      values[0] // => postsJSON
      values[1] // => commentsJSON
  
      return values;
    });
    ```
  
    @class Promise
    @param {Function} resolver
    Useful for tooling.
    @constructor
  */

  var Promise$2 = function () {
    function Promise(resolver) {
      this[PROMISE_ID] = nextId();
      this._result = this._state = undefined;
      this._subscribers = [];

      if (noop !== resolver) {
        typeof resolver !== 'function' && needsResolver();
        this instanceof Promise ? initializePromise(this, resolver) : needsNew();
      }
    }

    /**
    The primary way of interacting with a promise is through its `then` method,
    which registers callbacks to receive either a promise's eventual value or the
    reason why the promise cannot be fulfilled.
     ```js
    findUser().then(function(user){
      // user is available
    }, function(reason){
      // user is unavailable, and you are given the reason why
    });
    ```
     Chaining
    --------
     The return value of `then` is itself a promise.  This second, 'downstream'
    promise is resolved with the return value of the first promise's fulfillment
    or rejection handler, or rejected if the handler throws an exception.
     ```js
    findUser().then(function (user) {
      return user.name;
    }, function (reason) {
      return 'default name';
    }).then(function (userName) {
      // If `findUser` fulfilled, `userName` will be the user's name, otherwise it
      // will be `'default name'`
    });
     findUser().then(function (user) {
      throw new Error('Found user, but still unhappy');
    }, function (reason) {
      throw new Error('`findUser` rejected and we're unhappy');
    }).then(function (value) {
      // never reached
    }, function (reason) {
      // if `findUser` fulfilled, `reason` will be 'Found user, but still unhappy'.
      // If `findUser` rejected, `reason` will be '`findUser` rejected and we're unhappy'.
    });
    ```
    If the downstream promise does not specify a rejection handler, rejection reasons will be propagated further downstream.
     ```js
    findUser().then(function (user) {
      throw new PedagogicalException('Upstream error');
    }).then(function (value) {
      // never reached
    }).then(function (value) {
      // never reached
    }, function (reason) {
      // The `PedgagocialException` is propagated all the way down to here
    });
    ```
     Assimilation
    ------------
     Sometimes the value you want to propagate to a downstream promise can only be
    retrieved asynchronously. This can be achieved by returning a promise in the
    fulfillment or rejection handler. The downstream promise will then be pending
    until the returned promise is settled. This is called *assimilation*.
     ```js
    findUser().then(function (user) {
      return findCommentsByAuthor(user);
    }).then(function (comments) {
      // The user's comments are now available
    });
    ```
     If the assimliated promise rejects, then the downstream promise will also reject.
     ```js
    findUser().then(function (user) {
      return findCommentsByAuthor(user);
    }).then(function (comments) {
      // If `findCommentsByAuthor` fulfills, we'll have the value here
    }, function (reason) {
      // If `findCommentsByAuthor` rejects, we'll have the reason here
    });
    ```
     Simple Example
    --------------
     Synchronous Example
     ```javascript
    let result;
     try {
      result = findResult();
      // success
    } catch(reason) {
      // failure
    }
    ```
     Errback Example
     ```js
    findResult(function(result, err){
      if (err) {
        // failure
      } else {
        // success
      }
    });
    ```
     Promise Example;
     ```javascript
    findResult().then(function(result){
      // success
    }, function(reason){
      // failure
    });
    ```
     Advanced Example
    --------------
     Synchronous Example
     ```javascript
    let author, books;
     try {
      author = findAuthor();
      books  = findBooksByAuthor(author);
      // success
    } catch(reason) {
      // failure
    }
    ```
     Errback Example
     ```js
     function foundBooks(books) {
     }
     function failure(reason) {
     }
     findAuthor(function(author, err){
      if (err) {
        failure(err);
        // failure
      } else {
        try {
          findBoooksByAuthor(author, function(books, err) {
            if (err) {
              failure(err);
            } else {
              try {
                foundBooks(books);
              } catch(reason) {
                failure(reason);
              }
            }
          });
        } catch(error) {
          failure(err);
        }
        // success
      }
    });
    ```
     Promise Example;
     ```javascript
    findAuthor().
      then(findBooksByAuthor).
      then(function(books){
        // found books
    }).catch(function(reason){
      // something went wrong
    });
    ```
     @method then
    @param {Function} onFulfilled
    @param {Function} onRejected
    Useful for tooling.
    @return {Promise}
    */

    /**
    `catch` is simply sugar for `then(undefined, onRejection)` which makes it the same
    as the catch block of a try/catch statement.
    ```js
    function findAuthor(){
    throw new Error('couldn't find that author');
    }
    // synchronous
    try {
    findAuthor();
    } catch(reason) {
    // something went wrong
    }
    // async with promises
    findAuthor().catch(function(reason){
    // something went wrong
    });
    ```
    @method catch
    @param {Function} onRejection
    Useful for tooling.
    @return {Promise}
    */

    Promise.prototype.catch = function _catch(onRejection) {
      return this.then(null, onRejection);
    };

    /**
      `finally` will be invoked regardless of the promise's fate just as native
      try/catch/finally behaves
    
      Synchronous example:
    
      ```js
      findAuthor() {
        if (Math.random() > 0.5) {
          throw new Error();
        }
        return new Author();
      }
    
      try {
        return findAuthor(); // succeed or fail
      } catch(error) {
        return findOtherAuther();
      } finally {
        // always runs
        // doesn't affect the return value
      }
      ```
    
      Asynchronous example:
    
      ```js
      findAuthor().catch(function(reason){
        return findOtherAuther();
      }).finally(function(){
        // author was either found, or not
      });
      ```
    
      @method finally
      @param {Function} callback
      @return {Promise}
    */

    Promise.prototype.finally = function _finally(callback) {
      var promise = this;
      var constructor = promise.constructor;

      return promise.then(function (value) {
        return constructor.resolve(callback()).then(function () {
          return value;
        });
      }, function (reason) {
        return constructor.resolve(callback()).then(function () {
          throw reason;
        });
      });
    };

    return Promise;
  }();

  Promise$2.prototype.then = then;
  Promise$2.all = all;
  Promise$2.race = race;
  Promise$2.resolve = resolve$1;
  Promise$2.reject = reject$1;
  Promise$2._setScheduler = setScheduler;
  Promise$2._setAsap = setAsap;
  Promise$2._asap = asap;

  /*global self*/
  function polyfill() {
    var local = void 0;

    if (typeof global !== 'undefined') {
      local = global;
    } else if (typeof self !== 'undefined') {
      local = self;
    } else {
      try {
        local = Function('return this')();
      } catch (e) {
        throw new Error('polyfill failed because global object is unavailable in this environment');
      }
    }

    var P = local.Promise;

    if (P) {
      var promiseToString = null;
      try {
        promiseToString = Object.prototype.toString.call(P.resolve());
      } catch (e) {
        // silently ignored
      }

      if (promiseToString === '[object Promise]' && !P.cast) {
        return;
      }
    }

    local.Promise = Promise$2;
  }

  // Strange compat..
  Promise$2.polyfill = polyfill;
  Promise$2.Promise = Promise$2;

  Promise$2.polyfill();

  return Promise$2;
});

//# sourceMappingURL=es6-promise.auto.map
/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(94), __webpack_require__(95)))

/***/ }),
/* 94 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


// shim for using process in browser
var process = module.exports = {};

// cached from whatever global is present so that test runners that stub it
// don't break things.  But we need to wrap it in a try catch in case it is
// wrapped in strict mode code which doesn't define any globals.  It's inside a
// function because try/catches deoptimize in certain engines.

var cachedSetTimeout;
var cachedClearTimeout;

function defaultSetTimout() {
    throw new Error('setTimeout has not been defined');
}
function defaultClearTimeout() {
    throw new Error('clearTimeout has not been defined');
}
(function () {
    try {
        if (typeof setTimeout === 'function') {
            cachedSetTimeout = setTimeout;
        } else {
            cachedSetTimeout = defaultSetTimout;
        }
    } catch (e) {
        cachedSetTimeout = defaultSetTimout;
    }
    try {
        if (typeof clearTimeout === 'function') {
            cachedClearTimeout = clearTimeout;
        } else {
            cachedClearTimeout = defaultClearTimeout;
        }
    } catch (e) {
        cachedClearTimeout = defaultClearTimeout;
    }
})();
function runTimeout(fun) {
    if (cachedSetTimeout === setTimeout) {
        //normal enviroments in sane situations
        return setTimeout(fun, 0);
    }
    // if setTimeout wasn't available but was latter defined
    if ((cachedSetTimeout === defaultSetTimout || !cachedSetTimeout) && setTimeout) {
        cachedSetTimeout = setTimeout;
        return setTimeout(fun, 0);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedSetTimeout(fun, 0);
    } catch (e) {
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't trust the global object when called normally
            return cachedSetTimeout.call(null, fun, 0);
        } catch (e) {
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error
            return cachedSetTimeout.call(this, fun, 0);
        }
    }
}
function runClearTimeout(marker) {
    if (cachedClearTimeout === clearTimeout) {
        //normal enviroments in sane situations
        return clearTimeout(marker);
    }
    // if clearTimeout wasn't available but was latter defined
    if ((cachedClearTimeout === defaultClearTimeout || !cachedClearTimeout) && clearTimeout) {
        cachedClearTimeout = clearTimeout;
        return clearTimeout(marker);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedClearTimeout(marker);
    } catch (e) {
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't  trust the global object when called normally
            return cachedClearTimeout.call(null, marker);
        } catch (e) {
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error.
            // Some versions of I.E. have different rules for clearTimeout vs setTimeout
            return cachedClearTimeout.call(this, marker);
        }
    }
}
var queue = [];
var draining = false;
var currentQueue;
var queueIndex = -1;

function cleanUpNextTick() {
    if (!draining || !currentQueue) {
        return;
    }
    draining = false;
    if (currentQueue.length) {
        queue = currentQueue.concat(queue);
    } else {
        queueIndex = -1;
    }
    if (queue.length) {
        drainQueue();
    }
}

function drainQueue() {
    if (draining) {
        return;
    }
    var timeout = runTimeout(cleanUpNextTick);
    draining = true;

    var len = queue.length;
    while (len) {
        currentQueue = queue;
        queue = [];
        while (++queueIndex < len) {
            if (currentQueue) {
                currentQueue[queueIndex].run();
            }
        }
        queueIndex = -1;
        len = queue.length;
    }
    currentQueue = null;
    draining = false;
    runClearTimeout(timeout);
}

process.nextTick = function (fun) {
    var args = new Array(arguments.length - 1);
    if (arguments.length > 1) {
        for (var i = 1; i < arguments.length; i++) {
            args[i - 1] = arguments[i];
        }
    }
    queue.push(new Item(fun, args));
    if (queue.length === 1 && !draining) {
        runTimeout(drainQueue);
    }
};

// v8 likes predictible objects
function Item(fun, array) {
    this.fun = fun;
    this.array = array;
}
Item.prototype.run = function () {
    this.fun.apply(null, this.array);
};
process.title = 'browser';
process.browser = true;
process.env = {};
process.argv = [];
process.version = ''; // empty string to avoid regexp issues
process.versions = {};

function noop() {}

process.on = noop;
process.addListener = noop;
process.once = noop;
process.off = noop;
process.removeListener = noop;
process.removeAllListeners = noop;
process.emit = noop;
process.prependListener = noop;
process.prependOnceListener = noop;

process.listeners = function (name) {
    return [];
};

process.binding = function (name) {
    throw new Error('process.binding is not supported');
};

process.cwd = function () {
    return '/';
};
process.chdir = function (dir) {
    throw new Error('process.chdir is not supported');
};
process.umask = function () {
    return 0;
};

/***/ }),
/* 95 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

var g;

// This works in non-strict mode
g = function () {
	return this;
}();

try {
	// This works if eval is allowed (see CSP)
	g = g || Function("return this")() || (1, eval)("this");
} catch (e) {
	// This works if the window reference is available
	if ((typeof window === "undefined" ? "undefined" : _typeof(window)) === "object") g = window;
}

// g can still be undefined, but nothing to do about it...
// We return undefined, instead of nothing here, so it's
// easier to handle this case. if(!global) { ...}

module.exports = g;

/***/ }),
/* 96 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


(function (self) {
  'use strict';

  if (self.fetch) {
    return;
  }

  var support = {
    searchParams: 'URLSearchParams' in self,
    iterable: 'Symbol' in self && 'iterator' in Symbol,
    blob: 'FileReader' in self && 'Blob' in self && function () {
      try {
        new Blob();
        return true;
      } catch (e) {
        return false;
      }
    }(),
    formData: 'FormData' in self,
    arrayBuffer: 'ArrayBuffer' in self
  };

  if (support.arrayBuffer) {
    var viewClasses = ['[object Int8Array]', '[object Uint8Array]', '[object Uint8ClampedArray]', '[object Int16Array]', '[object Uint16Array]', '[object Int32Array]', '[object Uint32Array]', '[object Float32Array]', '[object Float64Array]'];

    var isDataView = function isDataView(obj) {
      return obj && DataView.prototype.isPrototypeOf(obj);
    };

    var isArrayBufferView = ArrayBuffer.isView || function (obj) {
      return obj && viewClasses.indexOf(Object.prototype.toString.call(obj)) > -1;
    };
  }

  function normalizeName(name) {
    if (typeof name !== 'string') {
      name = String(name);
    }
    if (/[^a-z0-9\-#$%&'*+.\^_`|~]/i.test(name)) {
      throw new TypeError('Invalid character in header field name');
    }
    return name.toLowerCase();
  }

  function normalizeValue(value) {
    if (typeof value !== 'string') {
      value = String(value);
    }
    return value;
  }

  // Build a destructive iterator for the value list
  function iteratorFor(items) {
    var iterator = {
      next: function next() {
        var value = items.shift();
        return { done: value === undefined, value: value };
      }
    };

    if (support.iterable) {
      iterator[Symbol.iterator] = function () {
        return iterator;
      };
    }

    return iterator;
  }

  function Headers(headers) {
    this.map = {};

    if (headers instanceof Headers) {
      headers.forEach(function (value, name) {
        this.append(name, value);
      }, this);
    } else if (Array.isArray(headers)) {
      headers.forEach(function (header) {
        this.append(header[0], header[1]);
      }, this);
    } else if (headers) {
      Object.getOwnPropertyNames(headers).forEach(function (name) {
        this.append(name, headers[name]);
      }, this);
    }
  }

  Headers.prototype.append = function (name, value) {
    name = normalizeName(name);
    value = normalizeValue(value);
    var oldValue = this.map[name];
    this.map[name] = oldValue ? oldValue + ',' + value : value;
  };

  Headers.prototype['delete'] = function (name) {
    delete this.map[normalizeName(name)];
  };

  Headers.prototype.get = function (name) {
    name = normalizeName(name);
    return this.has(name) ? this.map[name] : null;
  };

  Headers.prototype.has = function (name) {
    return this.map.hasOwnProperty(normalizeName(name));
  };

  Headers.prototype.set = function (name, value) {
    this.map[normalizeName(name)] = normalizeValue(value);
  };

  Headers.prototype.forEach = function (callback, thisArg) {
    for (var name in this.map) {
      if (this.map.hasOwnProperty(name)) {
        callback.call(thisArg, this.map[name], name, this);
      }
    }
  };

  Headers.prototype.keys = function () {
    var items = [];
    this.forEach(function (value, name) {
      items.push(name);
    });
    return iteratorFor(items);
  };

  Headers.prototype.values = function () {
    var items = [];
    this.forEach(function (value) {
      items.push(value);
    });
    return iteratorFor(items);
  };

  Headers.prototype.entries = function () {
    var items = [];
    this.forEach(function (value, name) {
      items.push([name, value]);
    });
    return iteratorFor(items);
  };

  if (support.iterable) {
    Headers.prototype[Symbol.iterator] = Headers.prototype.entries;
  }

  function consumed(body) {
    if (body.bodyUsed) {
      return Promise.reject(new TypeError('Already read'));
    }
    body.bodyUsed = true;
  }

  function fileReaderReady(reader) {
    return new Promise(function (resolve, reject) {
      reader.onload = function () {
        resolve(reader.result);
      };
      reader.onerror = function () {
        reject(reader.error);
      };
    });
  }

  function readBlobAsArrayBuffer(blob) {
    var reader = new FileReader();
    var promise = fileReaderReady(reader);
    reader.readAsArrayBuffer(blob);
    return promise;
  }

  function readBlobAsText(blob) {
    var reader = new FileReader();
    var promise = fileReaderReady(reader);
    reader.readAsText(blob);
    return promise;
  }

  function readArrayBufferAsText(buf) {
    var view = new Uint8Array(buf);
    var chars = new Array(view.length);

    for (var i = 0; i < view.length; i++) {
      chars[i] = String.fromCharCode(view[i]);
    }
    return chars.join('');
  }

  function bufferClone(buf) {
    if (buf.slice) {
      return buf.slice(0);
    } else {
      var view = new Uint8Array(buf.byteLength);
      view.set(new Uint8Array(buf));
      return view.buffer;
    }
  }

  function Body() {
    this.bodyUsed = false;

    this._initBody = function (body) {
      this._bodyInit = body;
      if (!body) {
        this._bodyText = '';
      } else if (typeof body === 'string') {
        this._bodyText = body;
      } else if (support.blob && Blob.prototype.isPrototypeOf(body)) {
        this._bodyBlob = body;
      } else if (support.formData && FormData.prototype.isPrototypeOf(body)) {
        this._bodyFormData = body;
      } else if (support.searchParams && URLSearchParams.prototype.isPrototypeOf(body)) {
        this._bodyText = body.toString();
      } else if (support.arrayBuffer && support.blob && isDataView(body)) {
        this._bodyArrayBuffer = bufferClone(body.buffer);
        // IE 10-11 can't handle a DataView body.
        this._bodyInit = new Blob([this._bodyArrayBuffer]);
      } else if (support.arrayBuffer && (ArrayBuffer.prototype.isPrototypeOf(body) || isArrayBufferView(body))) {
        this._bodyArrayBuffer = bufferClone(body);
      } else {
        throw new Error('unsupported BodyInit type');
      }

      if (!this.headers.get('content-type')) {
        if (typeof body === 'string') {
          this.headers.set('content-type', 'text/plain;charset=UTF-8');
        } else if (this._bodyBlob && this._bodyBlob.type) {
          this.headers.set('content-type', this._bodyBlob.type);
        } else if (support.searchParams && URLSearchParams.prototype.isPrototypeOf(body)) {
          this.headers.set('content-type', 'application/x-www-form-urlencoded;charset=UTF-8');
        }
      }
    };

    if (support.blob) {
      this.blob = function () {
        var rejected = consumed(this);
        if (rejected) {
          return rejected;
        }

        if (this._bodyBlob) {
          return Promise.resolve(this._bodyBlob);
        } else if (this._bodyArrayBuffer) {
          return Promise.resolve(new Blob([this._bodyArrayBuffer]));
        } else if (this._bodyFormData) {
          throw new Error('could not read FormData body as blob');
        } else {
          return Promise.resolve(new Blob([this._bodyText]));
        }
      };

      this.arrayBuffer = function () {
        if (this._bodyArrayBuffer) {
          return consumed(this) || Promise.resolve(this._bodyArrayBuffer);
        } else {
          return this.blob().then(readBlobAsArrayBuffer);
        }
      };
    }

    this.text = function () {
      var rejected = consumed(this);
      if (rejected) {
        return rejected;
      }

      if (this._bodyBlob) {
        return readBlobAsText(this._bodyBlob);
      } else if (this._bodyArrayBuffer) {
        return Promise.resolve(readArrayBufferAsText(this._bodyArrayBuffer));
      } else if (this._bodyFormData) {
        throw new Error('could not read FormData body as text');
      } else {
        return Promise.resolve(this._bodyText);
      }
    };

    if (support.formData) {
      this.formData = function () {
        return this.text().then(decode);
      };
    }

    this.json = function () {
      return this.text().then(JSON.parse);
    };

    return this;
  }

  // HTTP methods whose capitalization should be normalized
  var methods = ['DELETE', 'GET', 'HEAD', 'OPTIONS', 'POST', 'PUT'];

  function normalizeMethod(method) {
    var upcased = method.toUpperCase();
    return methods.indexOf(upcased) > -1 ? upcased : method;
  }

  function Request(input, options) {
    options = options || {};
    var body = options.body;

    if (input instanceof Request) {
      if (input.bodyUsed) {
        throw new TypeError('Already read');
      }
      this.url = input.url;
      this.credentials = input.credentials;
      if (!options.headers) {
        this.headers = new Headers(input.headers);
      }
      this.method = input.method;
      this.mode = input.mode;
      if (!body && input._bodyInit != null) {
        body = input._bodyInit;
        input.bodyUsed = true;
      }
    } else {
      this.url = String(input);
    }

    this.credentials = options.credentials || this.credentials || 'omit';
    if (options.headers || !this.headers) {
      this.headers = new Headers(options.headers);
    }
    this.method = normalizeMethod(options.method || this.method || 'GET');
    this.mode = options.mode || this.mode || null;
    this.referrer = null;

    if ((this.method === 'GET' || this.method === 'HEAD') && body) {
      throw new TypeError('Body not allowed for GET or HEAD requests');
    }
    this._initBody(body);
  }

  Request.prototype.clone = function () {
    return new Request(this, { body: this._bodyInit });
  };

  function decode(body) {
    var form = new FormData();
    body.trim().split('&').forEach(function (bytes) {
      if (bytes) {
        var split = bytes.split('=');
        var name = split.shift().replace(/\+/g, ' ');
        var value = split.join('=').replace(/\+/g, ' ');
        form.append(decodeURIComponent(name), decodeURIComponent(value));
      }
    });
    return form;
  }

  function parseHeaders(rawHeaders) {
    var headers = new Headers();
    rawHeaders.split(/\r?\n/).forEach(function (line) {
      var parts = line.split(':');
      var key = parts.shift().trim();
      if (key) {
        var value = parts.join(':').trim();
        headers.append(key, value);
      }
    });
    return headers;
  }

  Body.call(Request.prototype);

  function Response(bodyInit, options) {
    if (!options) {
      options = {};
    }

    this.type = 'default';
    this.status = 'status' in options ? options.status : 200;
    this.ok = this.status >= 200 && this.status < 300;
    this.statusText = 'statusText' in options ? options.statusText : 'OK';
    this.headers = new Headers(options.headers);
    this.url = options.url || '';
    this._initBody(bodyInit);
  }

  Body.call(Response.prototype);

  Response.prototype.clone = function () {
    return new Response(this._bodyInit, {
      status: this.status,
      statusText: this.statusText,
      headers: new Headers(this.headers),
      url: this.url
    });
  };

  Response.error = function () {
    var response = new Response(null, { status: 0, statusText: '' });
    response.type = 'error';
    return response;
  };

  var redirectStatuses = [301, 302, 303, 307, 308];

  Response.redirect = function (url, status) {
    if (redirectStatuses.indexOf(status) === -1) {
      throw new RangeError('Invalid status code');
    }

    return new Response(null, { status: status, headers: { location: url } });
  };

  self.Headers = Headers;
  self.Request = Request;
  self.Response = Response;

  self.fetch = function (input, init) {
    return new Promise(function (resolve, reject) {
      var request = new Request(input, init);
      var xhr = new XMLHttpRequest();

      xhr.onload = function () {
        var options = {
          status: xhr.status,
          statusText: xhr.statusText,
          headers: parseHeaders(xhr.getAllResponseHeaders() || '')
        };
        options.url = 'responseURL' in xhr ? xhr.responseURL : options.headers.get('X-Request-URL');
        var body = 'response' in xhr ? xhr.response : xhr.responseText;
        resolve(new Response(body, options));
      };

      xhr.onerror = function () {
        reject(new TypeError('Network request failed'));
      };

      xhr.ontimeout = function () {
        reject(new TypeError('Network request failed'));
      };

      xhr.open(request.method, request.url, true);

      if (request.credentials === 'include') {
        xhr.withCredentials = true;
      }

      if ('responseType' in xhr && support.blob) {
        xhr.responseType = 'blob';
      }

      request.headers.forEach(function (value, name) {
        xhr.setRequestHeader(name, value);
      });

      xhr.send(typeof request._bodyInit === 'undefined' ? null : request._bodyInit);
    });
  };
  self.fetch.polyfill = true;
})(typeof self !== 'undefined' ? self : undefined);

/***/ }),
/* 97 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.toDOM = toDOM;

var _element = __webpack_require__(22);

var _fragment = __webpack_require__(62);

var _nodes = __webpack_require__(17);

var _html = __webpack_require__(48);

function toDOM(data) {
  return (0, _fragment.fragment)((0, _nodes.nodes)((0, _html.html)((0, _element.element)('body'), data)));
}

/***/ }),
/* 98 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isCollection = isCollection;

var _global = __webpack_require__(9);

var window = (0, _global.global)(),
    HTMLCollection = window.HTMLCollection,
    NodeList = window.NodeList;

function isCollection(contents) {
  return contents instanceof HTMLCollection || contents instanceof NodeList;
}

/***/ }),
/* 99 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.wrapAll = wrapAll;

var _wrap = __webpack_require__(63);

var _forEach = __webpack_require__(21);

var _slice = __webpack_require__(47);

function wrapAll(nodes, wrapper) {
  (0, _forEach.forEach)((0, _slice.slice)(nodes, 0), function (node) {
    (0, _wrap.wrap)(node, wrapper);
  });

  return wrapper;
}

/***/ }),
/* 100 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.call = call;
function call(method, event, element, listener, useCapture) {
  if ('on'.concat(event) in element) {
    method.call(element, event, listener, useCapture);
  }
}

/***/ }),
/* 101 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.rects = rects;
function rects(node) {
  return node.getClientRects();
}

/***/ }),
/* 102 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.request = request;

var _create = __webpack_require__(14);

var _one = __webpack_require__(6);

var _queue = __webpack_require__(67);

var _global = __webpack_require__(9);

var window = (0, _global.global)(),
    fetch = window.fetch,
    prototype = (0, _create.create)(),
    selector = 'input[type=submit]:focus,' + 'button[type=submit]:focus,' + 'button:not([type]):focus,' + 'input[type=submit]:hover,' + 'button[type=submit]:hover,' + 'button:not([type]):hover,' + ':not(input):hover,' + 'input[type=submit],' + 'button[type=submit],' + 'button:not([type])';

/**
 * Adds a field value on an existing body
 * @param {String} name
 * @param {*} value
 * @return {Object}
 */
prototype.body = function (name, value) {
  this.options.body.append(name, value);

  return this;
};

/**
 * Adds credentials to the request
 * (let by default, if you fetch a resource on the same domain, if not, use 'include')
 * @param {String} [value='same-origin']
 * @return {Object}
 */
prototype.credentials = function (value) {
  this.options.credentials = value || 'same-origin';

  return this;
};

/**
 * Fetches the request & adds a trigger callback for the response
 * @param {Function} trigger
 * @returns {Promise}
 */
prototype.fetch = function (trigger) {
  var item = (0, _create.create)();

  item.request = this;
  _queue.queue.push(item);

  return new Promise(function (resolve, reject) {
    item.request.resolve = resolve;
    item.reject = reject;
    item.trigger = trigger;
    item.request.fetchRequest();
  });
};

/**
 * Retries to fetch a request
 * @returns {Object}
 */
prototype.retry = function () {
  if (!_queue.queue[0] || _queue.queue[0].request !== this) {
    return this;
  }

  _queue.queue.unshift(1);
  _queue.queue.next();

  return this;
};

/**
 * Add a header to the request
 * @param {String} name
 * @param {String} value
 * @returns {Object}
 */
prototype.header = function (name, value) {
  this.options.headers[name] = value;

  return this;
};

/**
 * Adds an option to the request
 * @param {String} name
 * @param {String} value
 * @returns {Object}
 */
prototype.option = function (name, value) {
  this.options[name] = value;

  return this;
};

prototype.fetchRequest = function () {
  var item = _queue.queue[0];

  if (_queue.queue[1]) {
    return;
  }

  return fetch(item.request.url, item.request.options).then(item.trigger || item.request.resolve).then(_queue.queue.next)['catch'](item.reject);
};

/**
 * Builds a request
 * @param {String} url
 * @param {String} method (get or post)
 * @param {Object} [body] (the post request body)
 * @param {Element} [target] (the event target)
 * @return {Object}
 */
function request(url, method, body, target) {
  var request = void 0,
      options = void 0;

  request = (0, _create.create)(prototype);
  options = (0, _create.create)();

  request.options = options;

  if (target) {
    request.target = target;
    request.originalTarget = (0, _one.one)(selector, target.ownerDocument);
  }

  request.url = url;

  options.headers = (0, _create.create)();
  options.headers['X-Requested-With'] = 'XMLHttpRequest';
  options.method = method;

  if (method === 'post') {
    options.body = body;
  }

  return request;
}

/***/ }),
/* 103 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _onClick = __webpack_require__(2);

var _onSubmit = __webpack_require__(66);

__webpack_require__(104);

var a1 = ' a:not([download]):not([target]):not([href^="data:"])',
    a2 = ' a[target=_self]:not([download]):not([href^="data:"])',
    interactive = ['body > header' + a1, 'body > header' + a2, 'main > :not(form)' + a1, 'main > :not(form)' + a2, 'body > footer' + a1, 'body > footer' + a2, 'button[data-href]'].join(',');

_anticore.anticore.on(interactive, function (element, next) {
  (0, _onClick.onClick)(element, _anticore.anticore.fetchFromEvent);

  next();
});

_anticore.anticore.on('form:not([target]),form[target=_self]', function (element, next) {
  (0, _onSubmit.onSubmit)(element, _anticore.anticore.cleanAndFetch);
  next();
});

_anticore.anticore.fetchers.a = _anticore.anticore.fetchers.button = function (element) {
  return _anticore.anticore.request(element.dataset.href || element.href, 'get', null, element);
};

_anticore.anticore.populate();

/***/ }),
/* 104 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


__webpack_require__(105);

/***/ }),
/* 105 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


__webpack_require__(106);

__webpack_require__(132);

__webpack_require__(161);

__webpack_require__(171);

/***/ }),
/* 106 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


__webpack_require__(107);

__webpack_require__(121);

__webpack_require__(126);

/***/ }),
/* 107 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


__webpack_require__(108);

__webpack_require__(109);

__webpack_require__(110);

__webpack_require__(111);

__webpack_require__(112);

__webpack_require__(119);

__webpack_require__(120);

/***/ }),
/* 108 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _onClick = __webpack_require__(2);

function onClickEvent(event) {}

_anticore.anticore.on('form article .options button.delete', function (element, next) {
  (0, _onClick.onClick)(element, onClickEvent);

  next();
});

/***/ }),
/* 109 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _onClick = __webpack_require__(2);

function onClickEvent(event) {}

_anticore.anticore.on('form article .options button.edit', function (element, next) {
  (0, _onClick.onClick)(element, onClickEvent);

  next();
});

/***/ }),
/* 110 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _onClick = __webpack_require__(2);

function onClickEvent(event) {}

_anticore.anticore.on('form article .options button.publish', function (element, next) {
  (0, _onClick.onClick)(element, onClickEvent);

  next();
});

/***/ }),
/* 111 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _onClick = __webpack_require__(2);

function onClickEvent(event) {
  event.target.ownerDocument.execCommand('redo');
}

_anticore.anticore.on('form article .options button.redo', function (element, next) {
  (0, _onClick.onClick)(element, onClickEvent);

  next();
});

/***/ }),
/* 112 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _forEach = __webpack_require__(21);

var _anticore = __webpack_require__(0);

var _onClick = __webpack_require__(2);

var _prevent = __webpack_require__(1);

var _all = __webpack_require__(16);

var _closest = __webpack_require__(11);

var _one = __webpack_require__(6);

var _clean = __webpack_require__(7);

var _clone = __webpack_require__(68);

var _remove = __webpack_require__(5);

function onClickEvent(event) {
  var form = (0, _closest.closest)('form', event.target),
      article = (0, _one.one)('article', form),
      container = void 0;

  (0, _clean.clean)(article);
  container = (0, _clone.clone)(article, true);

  form.elements.article.value = container.outerHTML;
  (0, _prevent.prevent)(event);
  console.log(form.elements.article.value.replace(/<br>/g, '<br />'));
}

_anticore.anticore.on('form button:not([type])', function (element, next) {
  (0, _onClick.onClick)(element, onClickEvent);

  next();
});

/***/ }),
/* 113 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.returns = returns;

var _all = __webpack_require__(16);

function returns(node) {
  return (0, _all.all)('br', node);
}

/***/ }),
/* 114 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.styles = styles;

var _all = __webpack_require__(16);

function styles(node) {
  return (0, _all.all)('b, i, s, u', node);
}

/***/ }),
/* 115 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isTrailingStyle = isTrailingStyle;

var _nodeName = __webpack_require__(18);

var _isEmpty = __webpack_require__(25);

var _indexOf = __webpack_require__(10);

function isTrailingStyle(node) {
  return node && (0, _indexOf.indexOf)(['b', 'i', 's', 'u'], (0, _nodeName.nodeName)(node)) > -1 && (0, _isEmpty.isEmpty)(node);
}

/***/ }),
/* 116 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isTrailingReturn = isTrailingReturn;

var _nodeName = __webpack_require__(18);

var _isEmpty = __webpack_require__(25);

var _parent = __webpack_require__(3);

var _previousNode = __webpack_require__(117);

var _nextNode = __webpack_require__(49);

var _isReturn = __webpack_require__(118);

function isTrailingReturn(node) {
  return (0, _isReturn.isReturn)(node) && ((0, _nodeName.nodeName)((0, _parent.parent)(node)) === 'h1' || isTrailing((0, _previousNode.previousNode)(node)) || isTrailing((0, _nextNode.nextNode)(node)));
}

function isTrailing(sibling) {
  return !sibling || (0, _isEmpty.isEmpty)(sibling) || (0, _isReturn.isReturn)(sibling);
}

/***/ }),
/* 117 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.previousNode = previousNode;
function previousNode(node) {
  return node.previousSibling;
}

/***/ }),
/* 118 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isReturn = isReturn;

var _index = __webpack_require__(18);

function isReturn(node) {
  return node && (0, _index.nodeName)(node) === 'br';
}

/***/ }),
/* 119 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _onClick = __webpack_require__(2);

function onClickEvent(event) {
  event.target.ownerDocument.execCommand('undo');
}

_anticore.anticore.on('form article .options button.undo', function (element, next) {
  (0, _onClick.onClick)(element, onClickEvent);

  next();
});

/***/ }),
/* 120 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _onClick = __webpack_require__(2);

function onClickEvent(event) {}

_anticore.anticore.on('form article .options button.unpublish', function (element, next) {
  (0, _onClick.onClick)(element, onClickEvent);

  next();
});

/***/ }),
/* 121 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


__webpack_require__(122);

__webpack_require__(123);

__webpack_require__(124);

__webpack_require__(125);

/***/ }),
/* 122 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _onClick = __webpack_require__(2);

var _isHeadingNode = __webpack_require__(30);

var _one = __webpack_require__(6);

var _current = __webpack_require__(4);

function onClickEvent(event) {
  var selection = (0, _current.current)();

  if ((0, _isHeadingNode.isHeadingNode)(selection.anchorNode)) {
    return;
  }

  if ((0, _isHeadingNode.isHeadingNode)(selection.focusNode)) {
    return;
  }

  // @todo enable/disable button
  (0, _one.one)().execCommand('bold');
}

_anticore.anticore.on('form.editor .options button.bold', function (element, next) {
  (0, _onClick.onClick)(element, onClickEvent);

  next();
});

/***/ }),
/* 123 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _onClick = __webpack_require__(2);

var _isHeadingNode = __webpack_require__(30);

var _one = __webpack_require__(6);

var _current = __webpack_require__(4);

function onClickEvent(event) {
  var selection = (0, _current.current)();

  if ((0, _isHeadingNode.isHeadingNode)(selection.anchorNode)) {
    return;
  }

  if ((0, _isHeadingNode.isHeadingNode)(selection.focusNode)) {
    return;
  }

  // @todo enable/disable button
  (0, _one.one)().execCommand('italic');
}

_anticore.anticore.on('form.editor .options button.italic', function (element, next) {
  (0, _onClick.onClick)(element, onClickEvent);

  next();
});

/***/ }),
/* 124 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _onClick = __webpack_require__(2);

var _isHeadingNode = __webpack_require__(30);

var _one = __webpack_require__(6);

var _current = __webpack_require__(4);

function onClickEvent(event) {
  var selection = (0, _current.current)();

  if ((0, _isHeadingNode.isHeadingNode)(selection.anchorNode)) {
    return;
  }

  if ((0, _isHeadingNode.isHeadingNode)(selection.focusNode)) {
    return;
  }

  // @todo enable/disable button
  (0, _one.one)().execCommand('strikeThrough');
}

_anticore.anticore.on('form.editor .options button.strike', function (element, next) {
  (0, _onClick.onClick)(element, onClickEvent);

  next();
});

/***/ }),
/* 125 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _onClick = __webpack_require__(2);

var _isHeadingNode = __webpack_require__(30);

var _one = __webpack_require__(6);

var _current = __webpack_require__(4);

function onClickEvent(event) {
  var selection = (0, _current.current)();

  if ((0, _isHeadingNode.isHeadingNode)(selection.anchorNode)) {
    return;
  }

  if ((0, _isHeadingNode.isHeadingNode)(selection.focusNode)) {
    return;
  }

  // @todo enable/disable button
  (0, _one.one)().execCommand('underline');
}

_anticore.anticore.on('form.editor .options button.underline', function (element, next) {
  (0, _onClick.onClick)(element, onClickEvent);

  next();
});

/***/ }),
/* 126 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


__webpack_require__(127);

__webpack_require__(128);

__webpack_require__(129);

/***/ }),
/* 127 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _anchor = __webpack_require__(33);

var _isCollapsed = __webpack_require__(34);

var _onClick = __webpack_require__(2);

var _prevent = __webpack_require__(1);

var _closest = __webpack_require__(11);

var _closestOrSelf = __webpack_require__(35);

function onClickEvent(event) {
  var node = _anchor.anchor.node();

  return (!(0, _isCollapsed.isCollapsed)() || !(0, _closestOrSelf.closestOrSelf)('[contenteditable=true]', node) || (0, _closestOrSelf.closestOrSelf)('h1, a, b, i, s, o', node) || !(0, _closest.closest)('article > section, article footer .sources', node)) && (0, _prevent.prevent)(event);
}

_anticore.anticore.on('form.editor .options button.anchor', function (element, next) {
  (0, _onClick.onClick)(element, onClickEvent);

  next();
});

/***/ }),
/* 128 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _index = __webpack_require__(11);

var _anchor = __webpack_require__(33);

var _isCollapsed = __webpack_require__(34);

var _closestOrSelf = __webpack_require__(35);

var _onClick = __webpack_require__(2);

var _prevent = __webpack_require__(1);

function onClickEvent(event) {
  var node = _anchor.anchor.node();

  return (!(0, _isCollapsed.isCollapsed)() || !(0, _closestOrSelf.closestOrSelf)('[contenteditable=true]', node) || (0, _closestOrSelf.closestOrSelf)('h1, a, b, i, s, o', node) || !(0, _index.closest)('article > section', node)) && (0, _prevent.prevent)(event);
}

_anticore.anticore.on('form.editor .options button.figure', function (element, next) {
  (0, _onClick.onClick)(element, onClickEvent);

  next();
});

/***/ }),
/* 129 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _closest = __webpack_require__(11);

var _closestOrSelf = __webpack_require__(35);

var _element = __webpack_require__(22);

var _append = __webpack_require__(19);

var _update = __webpack_require__(36);

var _onClick = __webpack_require__(2);

var _prevent = __webpack_require__(1);

var _anchor = __webpack_require__(33);

var _current = __webpack_require__(4);

var _isCollapsed = __webpack_require__(34);

function onClickEvent(event) {
  var node = _anchor.anchor.node(),
      toPrevent = !(0, _isCollapsed.isCollapsed)() || !(0, _closestOrSelf.closestOrSelf)('[contenteditable=true]', node) || (0, _closestOrSelf.closestOrSelf)('h1, a', node) || !(0, _closest.closest)('article > section', node);

  if (toPrevent) {
    return (0, _prevent.prevent)(event);
  }

  insertAnchor();
}

function insertAnchor() {
  var anchor = (0, _element.element)('a'),
      sup = (0, _element.element)('sup');

  (0, _append.append)(sup, anchor);

  insert((0, _update.update)(anchor, {
    contenteditable: false,
    href: '#'
  }));
}

function insert(node) {
  var selection = (0, _current.current)(),
      range = selection.getRangeAt(0);

  range.deleteContents();
  range.insertNode(node);
  selection.collapseToEnd();

  return selection;
}

_anticore.anticore.on('form.editor .options button.source', function (element, next) {
  (0, _onClick.onClick)(element, onClickEvent);

  next();
});

/***/ }),
/* 130 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

exports.isObject = isObject;
function isObject(value) {
  return (typeof value === 'undefined' ? 'undefined' : _typeof(value)) === 'object';
}

/***/ }),
/* 131 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.style = style;

var _global = __webpack_require__(9);

var _isString = __webpack_require__(41);

var _isNull = __webpack_require__(50);

var _isNumber = __webpack_require__(42);

var _demethodize = __webpack_require__(32);

var _camelToDash = __webpack_require__(70);

var _dashToCamel = __webpack_require__(71);

var getStyle = (0, _demethodize.demethodize)((0, _global.global)().getComputedStyle);

function style(element, name, value) {
  if ((0, _isString.isString)(value) || (0, _isNumber.isNumber)(value) || (0, _isNull.isNull)(value)) {
    element.style[(0, _dashToCamel.dashToCamel)(name)] = value;

    return element;
  }

  return getStyle(element)[(0, _camelToDash.camelToDash)(name)];
}

/***/ }),
/* 132 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


__webpack_require__(133);

__webpack_require__(135);

__webpack_require__(151);

/***/ }),
/* 133 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _index = __webpack_require__(72);

_anticore.anticore.on('header h1[contenteditable=true]', function (element, next) {
  (0, _index.listenH1)(element);
  next();
});

/***/ }),
/* 134 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.firstOrSelf = firstOrSelf;

var _firstNode = __webpack_require__(37);

function firstOrSelf(node) {
  return (0, _firstNode.firstNode)(node) ? firstOrSelf((0, _firstNode.firstNode)(node)) : node;
}

/***/ }),
/* 135 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _listenImg = __webpack_require__(136);

_anticore.anticore.on('article header img', function (element, next) {
  (0, _listenImg.listenImg)(element);

  next();
});

/***/ }),
/* 136 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.listenImg = listenImg;

var _onClick = __webpack_require__(2);

var _image = __webpack_require__(137);

function listenImg(element) {
  (0, _onClick.onClick)(element, (0, _image.image)(element));
}

/***/ }),
/* 137 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.image = image;

var _index = __webpack_require__(26);

var _index2 = __webpack_require__(22);

var _index3 = __webpack_require__(31);

var _index4 = __webpack_require__(43);

var _index5 = __webpack_require__(5);

var _index6 = __webpack_require__(36);

var _index7 = __webpack_require__(13);

var _index8 = __webpack_require__(14);

var _fromInput = __webpack_require__(75);

var _show = __webpack_require__(54);

var _nextOrClose = __webpack_require__(55);

var _onFile = __webpack_require__(79);

function image(element) {
  var instance = (0, _index8.create)();

  instance.element = element;
  instance.tooltip = tooltip(instance);

  return instance.tooltip.show;
}

function tooltip(handle) {
  var instance = (0, _index8.create)(),
      options = (0, _index8.create)();

  options.mime = 'image/jpeg';
  options.maxSize = 35000;
  options.quality = 1;
  options.width = 48;
  options.height = 48;

  instance.handle = handle;
  instance.show = (0, _index7.curry)(_show.show, instance);
  instance.close = (0, _index7.curry)(close, instance);
  instance.nextOrClose = (0, _index7.curry)(_nextOrClose.nextOrClose, instance);

  instance.element = (0, _index6.update)((0, _index2.element)('section'), {
    class: 'modal img'
  });

  instance.h1 = (0, _index6.update)((0, _index2.element)('h1'), {
    parent: instance.element,
    text: (0, _index4.data)(handle.element, 'h1')
  });

  (0, _index4.data)(handle.element, 'h1', null);

  instance.closer = (0, _index6.update)((0, _index2.element)('button'), {
    class: 'closer',
    text: '×',
    onClick: instance.close,
    parent: instance.element
  });

  instance.labels = [];
  instance.inputs = [];

  instance.labels.alt = (0, _index6.update)((0, _index2.element)('label'), {
    parent: instance.element,
    text: (0, _index4.data)(handle.element, 'alt')
  });
  instance.labels.push(instance.labels.alt);

  (0, _index4.data)(handle.element, 'alt', null);

  instance.inputs.alt = (0, _index6.update)((0, _index2.element)('input'), {
    parent: instance.labels.alt,
    onKeyUp: function onKeyUp(event) {
      (0, _index3.attr)(handle.element, 'alt', event.target.value);
    }
  });
  instance.inputs.push(instance.inputs.alt);

  (0, _index.onEnter)(instance.inputs.alt, instance.nextOrClose);

  instance.labels.src = (0, _index6.update)((0, _index2.element)('label'), {
    parent: instance.element,
    text: (0, _index4.data)(handle.element, 'src')
  });
  instance.labels.push(instance.labels.src);

  (0, _index4.data)(handle.element, 'src', null);

  instance.inputs.src = (0, _index6.update)((0, _index2.element)('input'), {
    parent: instance.labels.src,
    type: 'file',
    onKeyUp: function onKeyUp(event) {
      (0, _index3.attr)(handle.element, 'href', event.target.value);
    }
  });
  instance.inputs.push(instance.inputs.src);

  (0, _fromInput.fromInput)((0, _index7.curry)(_onFile.onFile, instance, options), instance.inputs.src);
  (0, _index.onEnter)(instance.inputs.src, instance.nextOrClose);

  return instance;
}

function close(tooltip) {
  var inputs = tooltip.inputs,
      alt = inputs.alt.value,
      src = inputs.src.value;

  if (!alt.length || !src.length) {
    (0, _index3.attr)(tooltip.handle.element, 'alt', null);
    (0, _index3.attr)(tooltip.handle.element, 'src', null);
  }

  (0, _index5.remove)(tooltip.element);
}

/***/ }),
/* 138 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onFileInput = onFileInput;

var _prevent = __webpack_require__(1);

var _onChange = __webpack_require__(139);

var _onDrop = __webpack_require__(140);

var _nodeName = __webpack_require__(18);

function trueListener(listener, event) {
  if (event.type === 'drop') {
    (0, _prevent.prevent)(event);
  }

  listener.call(this, event);
}

function onFileInput(element, listener, useCapture) {
  if ((0, _nodeName.nodeName)(element) === 'input' && element.type === 'file') {
    listener = trueListener.bind(element, listener);

    (0, _onChange.onChange)(element, listener, useCapture);
    (0, _onDrop.onDrop)(element, listener, useCapture);

    return listener;
  }
}

/***/ }),
/* 139 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onChange = onChange;

var _ = __webpack_require__(8);

function onChange(element, listener, useCapture) {
  return (0, _.on)('change', element, listener, useCapture);
}

/***/ }),
/* 140 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onDrop = onDrop;

var _ = __webpack_require__(8);

function onDrop(element, listener, useCapture) {
  return (0, _.on)('drop', element, listener, useCapture);
}

/***/ }),
/* 141 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.dataURLToImage = dataURLToImage;

var _index = __webpack_require__(60);

var _global = __webpack_require__(9);

var _onceLoad = __webpack_require__(142);

var _onceError = __webpack_require__(144);

var _promise = __webpack_require__(38);

var window = (0, _global.global)(),
    Image = window.Image;

function dataURLToImage(url) {
  return (0, _promise.promise)(read, url).then(_index.getTarget);
}

function read(url, resolve, reject) {
  var img = new Image();

  img.src = url;
  (0, _onceLoad.onceLoad)(img, resolve);
  (0, _onceError.onceError)(img, reject);
}

/***/ }),
/* 142 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onceLoad = onceLoad;

var _ = __webpack_require__(77);

function onceLoad(element, listener, useCapture) {
  return (0, _.once)('load', element, listener, useCapture);
}

/***/ }),
/* 143 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.off = undefined;

var _callEach = __webpack_require__(65);

var _global = __webpack_require__(9);

/**
 * Forgets a listener of an event on an element (touch or not)
 * @param {String} event
 * @param {Element} element
 * @param {Function} listener
 * @param {Boolean} useCapture
 * @returns {Function} listener
 */
var off = exports.off = _callEach.callEach.bind(null, (0, _global.global)().Element.prototype.removeEventListener);

/***/ }),
/* 144 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onceError = onceError;

var _ = __webpack_require__(77);

function onceError(element, listener, useCapture) {
  return (0, _.once)('error', element, listener, useCapture);
}

/***/ }),
/* 145 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.focus = focus;
function focus(input) {
  input.focus();
  input.selectionStart = input.selectionEnd = input.value.length;
}

/***/ }),
/* 146 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.compress = compress;

var _index = __webpack_require__(13);

var _index2 = __webpack_require__(80);

var _index3 = __webpack_require__(82);

function compress(options, img) {
  return (0, _index3.imageToCanvas)(img).then((0, _index.curry)(_index2.canvasToImage, options));
}

/***/ }),
/* 147 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.resize = resize;

var _element = __webpack_require__(22);

var _curry = __webpack_require__(13);

var _create = __webpack_require__(14);

var _promise = __webpack_require__(38);

var _canvasToImage = __webpack_require__(80);

var min = Math.min,
    max = Math.max;

function resize(options, img) {
  options = parse(img.naturalWidth, img.naturalHeight, options);

  return (0, _promise.promise)(toCanvas, options, img).then((0, _curry.curry)(_canvasToImage.canvasToImage, options));
}

function toCanvas(options, img, resolve) {
  var canvas = (0, _element.element)('canvas');

  canvas.width = options.width;
  canvas.height = options.height;
  draw(options, img, canvas);

  resolve(canvas);
}

function draw(options, img, canvas) {
  var context = canvas.getContext('2d');

  context.fillStyle = 'transparent';
  context.fillRect(0, 0, options.width, options.height);
  context.save();
  context.drawImage(img, 0, 0, img.width, img.height, 0, 0, options.width, options.height);
}

function parse(width, height, options) {
  var result = (0, _create.create)(),
      ratio = width / height;

  result.natural = normalize(width, height, ratio);
  result.max = normalize(options.maxWidth, options.maxHeight, ratio, Infinity);
  result.min = normalize(options.minWidth, options.minHeight, ratio, 0);
  result.fixed = normalize(options.width, options.height, ratio, Infinity);

  minMax('width', result);
  minMax('height', result);

  return result;
}

function normalize(width, height, ratio, defaultValue) {
  var config = (0, _create.create)();

  config.width = width || defaultValue;
  config.height = height || defaultValue;

  if (width && height) {
    if (height * ratio > width) {
      config.height = width / ratio;
    } else {
      config.width = height * ratio;
    }
  } else if (width) {
    config.height = width / ratio;
  } else if (height) {
    config.width = height * ratio;
  }

  return config;
}

function minMax(key, config) {
  config[key] = ~~min(config.max[key], max(config.fixed[key], config.min[key])) || config.natural[key];
}

/***/ }),
/* 148 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.imageToDataURL = imageToDataURL;

var _index = __webpack_require__(78);

var _index2 = __webpack_require__(149);

function imageToDataURL(options, img) {
  return (0, _index2.imageToBlob)(options, img).then(_index.blobToDataURL);
}

/***/ }),
/* 149 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.imageToBlob = imageToBlob;

var _curry = __webpack_require__(13);

var _index = __webpack_require__(81);

var _index2 = __webpack_require__(82);

function imageToBlob(options, img) {
  return (0, _index2.imageToCanvas)(img).then((0, _curry.curry)(_index.canvasToBlob, options));
}

/***/ }),
/* 150 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.display = display;
function display(tooltip, dataURL) {
  tooltip.handle.img.src = dataURL;
  tooltip.close();
}

/***/ }),
/* 151 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _listenP = __webpack_require__(39);

_anticore.anticore.on('header p[contenteditable=true]', function (element, next) {
  (0, _listenP.listenP)(element);
  next();
});

/***/ }),
/* 152 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onBlurEvent = onBlurEvent;

var _clean = __webpack_require__(7);

var _matches = __webpack_require__(23);

var _remove = __webpack_require__(5);

var _isFirstP = __webpack_require__(44);

var selector = 'form article section p:first-of-type';

function onBlurEvent(event) {
  var target = event.target;

  if (!(0, _clean.clean)(target) || (0, _isFirstP.isFirstP)(target) || (0, _matches.matches)(selector, target)) {
    return;
  }

  (0, _remove.remove)(target);
}

/***/ }),
/* 153 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onEnterEvent = onEnterEvent;

var _anchor = __webpack_require__(33);

var _isCollapsed = __webpack_require__(34);

var _start = __webpack_require__(53);

var _starts = __webpack_require__(45);

var _before = __webpack_require__(15);

var _listenP = __webpack_require__(39);

var _clean = __webpack_require__(7);

var _cut = __webpack_require__(57);

var _isFirstP = __webpack_require__(44);

var _nextElement = __webpack_require__(40);

var _parent = __webpack_require__(3);

var _prevent = __webpack_require__(1);

var _isEmpty = __webpack_require__(25);

function onEnterEvent(event) {
  var target = event.target,
      node = _anchor.anchor.node(),
      offset = _anchor.anchor.offset(),
      rest = void 0;

  if (isInvalid(target)) {
    return (0, _prevent.prevent)(event);
  }

  if (node !== target) {
    (0, _clean.clean)(target);

    return;
  }

  rest = (0, _cut.cut)(node, offset);
  rest.normalize();
  (0, _clean.clean)(target);
  (0, _clean.clean)(rest);
  (0, _listenP.listenP)(rest);
  (0, _before.before)(rest, (0, _nextElement.nextElement)(target), (0, _parent.parent)(target));
  (0, _start.start)(rest);
  (0, _prevent.prevent)(event);
}

function isInvalid(target) {
  var node = _anchor.anchor.node(),
      offset = _anchor.anchor.offset();

  return !(0, _isCollapsed.isCollapsed)() || (0, _isEmpty.isEmpty)(target) || (0, _isEmpty.isEmpty)(node) || (0, _isFirstP.isFirstP)(target) && (0, _starts.starts)(target) || !offset && node === target;
}

/***/ }),
/* 154 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.nextNodes = nextNodes;

var _slice = __webpack_require__(47);

var _indexOf = __webpack_require__(10);

var _parent = __webpack_require__(3);

var _nodes = __webpack_require__(17);

function nextNodes(node) {
  var siblings = (0, _nodes.nodes)((0, _parent.parent)(node));

  return (0, _slice.slice)(siblings, (0, _indexOf.indexOf)(siblings, node) + 1);
}

/***/ }),
/* 155 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onBackEvent = onBackEvent;

var _current = __webpack_require__(4);

var _previous = __webpack_require__(46);

var _starts = __webpack_require__(45);

var _appendAll = __webpack_require__(28);

var _clean = __webpack_require__(7);

var _remove = __webpack_require__(5);

var _isText = __webpack_require__(24);

var _isFirstP = __webpack_require__(44);

var _isHeadingNode = __webpack_require__(30);

var _editables = __webpack_require__(27);

var _parent = __webpack_require__(3);

var _nodes = __webpack_require__(17);

var _prevent = __webpack_require__(1);

function onBackEvent(event) {
  var target = event.target,
      selection = (0, _current.current)(),
      anchor = selection.anchorNode,
      node = void 0;

  if (!selection.isCollapsed) {
    return;
  }

  if (!(0, _starts.starts)(target)) {
    return;
  }

  if (!(0, _isFirstP.isFirstP)(target)) {
    node = (0, _previous.previous)(target, (0, _editables.editables)(target));

    if (node && !(0, _isHeadingNode.isHeadingNode)(node) && anchor) {
      if ((0, _isText.isText)(node)) {
        node = (0, _parent.parent)(node);
      }

      (0, _appendAll.appendAll)((0, _nodes.nodes)(target), node);
      (0, _clean.clean)(node);
      (0, _remove.remove)(target);
    }
  }

  (0, _prevent.prevent)(event);
}

/***/ }),
/* 156 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.lastOrSelf = lastOrSelf;

var _lastNode = __webpack_require__(88);

function lastOrSelf(node) {
  return (0, _lastNode.lastNode)(node) ? lastOrSelf((0, _lastNode.lastNode)(node)) : node;
}

/***/ }),
/* 157 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onDeleteEvent = onDeleteEvent;

var _isCollapsedToNext = __webpack_require__(89);

var _clean = __webpack_require__(7);

var _remove = __webpack_require__(5);

var _appendAll = __webpack_require__(28);

var _nextElement = __webpack_require__(40);

var _nodes = __webpack_require__(17);

var _prevent = __webpack_require__(1);

function onDeleteEvent(event) {
  var target = event.target,
      next = (0, _nextElement.nextElement)(target);

  if ((0, _isCollapsedToNext.isCollapsedToNext)(event)) {
    (0, _appendAll.appendAll)((0, _nodes.nodes)(next), target);
    (0, _clean.clean)(target);
    (0, _remove.remove)(next);
    (0, _prevent.prevent)(event);

    return;
  }
}

/***/ }),
/* 158 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.hasSameNodeName = hasSameNodeName;

var _nodeName = __webpack_require__(18);

function hasSameNodeName(original, target) {
  return (0, _nodeName.nodeName)(original) === (0, _nodeName.nodeName)(target);
}

/***/ }),
/* 159 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onLeftEvent = onLeftEvent;

var _current = __webpack_require__(4);

var _previous = __webpack_require__(46);

var _clean = __webpack_require__(7);

var _remove = __webpack_require__(5);

var _firstNode = __webpack_require__(37);

var _editables = __webpack_require__(27);

var _indexOf = __webpack_require__(10);

var _prevent = __webpack_require__(1);

function onLeftEvent(event) {
  var target = event.target,
      selection = (0, _current.current)(),
      anchor = selection.anchorNode,
      offset = selection.anchorOffset;

  if (!(0, _current.current)().isCollapsed) {
    return;
  }

  if ((0, _indexOf.indexOf)([target, (0, _firstNode.firstNode)(target)], anchor) > -1 && !offset) {
    (0, _previous.previous)(target, (0, _editables.editables)(target));

    if ((0, _clean.clean)(target)) {
      (0, _remove.remove)(target);
    }

    (0, _prevent.prevent)(event);
  }
}

/***/ }),
/* 160 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onRightEvent = onRightEvent;

var _current = __webpack_require__(4);

var _next = __webpack_require__(52);

var _clean = __webpack_require__(7);

var _remove = __webpack_require__(5);

var _text = __webpack_require__(12);

var _indexOf = __webpack_require__(10);

var _editables = __webpack_require__(27);

var _prevent = __webpack_require__(1);

function onRightEvent(event) {
  var target = event.target,
      selection = (0, _current.current)(),
      anchor = selection.anchorNode,
      offset = selection.anchorOffset;

  if (!(0, _current.current)().isCollapsed) {
    return;
  }

  if ((0, _indexOf.indexOf)([target, target.lastChild], anchor) > -1 && offset === (0, _text.text)(anchor).length) {
    (0, _next.next)(target, (0, _editables.editables)(target));

    if ((0, _clean.clean)(target)) {
      (0, _remove.remove)(target);
    }

    (0, _prevent.prevent)(event);
  }
}

/***/ }),
/* 161 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


__webpack_require__(162);

__webpack_require__(165);

__webpack_require__(169);

__webpack_require__(170);

var _anticore = __webpack_require__(0);

var _one = __webpack_require__(6);

var _before = __webpack_require__(15);

_anticore.anticore.on('section.content', function (element, next) {
  element.normalize();
  (0, _before.before)(element, (0, _one.one)('article > footer'));

  next();
});

/***/ }),
/* 162 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _listenA = __webpack_require__(163);

_anticore.anticore.on('a[data-href]', function (element, next) {
  (0, _listenA.listenA)(element);
  next();
});

/***/ }),
/* 163 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.listenA = listenA;

var _onClick = __webpack_require__(2);

var _anchor = __webpack_require__(164);

function listenA(element) {
  (0, _onClick.onClick)(element, (0, _anchor.anchor)(element));
}

/***/ }),
/* 164 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.anchor = anchor;

var _index = __webpack_require__(26);

var _index2 = __webpack_require__(24);

var _index3 = __webpack_require__(22);

var _index4 = __webpack_require__(17);

var _index5 = __webpack_require__(3);

var _index6 = __webpack_require__(4);

var _index7 = __webpack_require__(19);

var _index8 = __webpack_require__(28);

var _index9 = __webpack_require__(31);

var _index10 = __webpack_require__(43);

var _index11 = __webpack_require__(48);

var _index12 = __webpack_require__(5);

var _index13 = __webpack_require__(12);

var _index14 = __webpack_require__(36);

var _index15 = __webpack_require__(13);

var _index16 = __webpack_require__(14);

var _cut = __webpack_require__(57);

var _show = __webpack_require__(54);

var _nextOrClose = __webpack_require__(55);

function anchor(element) {
  var instance = (0, _index16.create)(),
      selection = (0, _index6.current)(),
      anchor = selection.anchorNode,
      siblings = (0, _index4.nodes)((0, _cut.cut)(anchor, selection.anchorOffset)),
      target = anchor;

  instance.element = element;
  instance.tooltip = tooltip(instance);

  if ((0, _index2.isText)(anchor)) {
    target = (0, _index5.parent)(anchor);
  }

  (0, _index7.append)(element, target);
  (0, _index8.appendAll)(siblings, target);

  instance.tooltip.show();

  return instance.tooltip.show;
}

function tooltip(handle) {
  var instance = (0, _index16.create)();

  instance.handle = handle;
  instance.show = (0, _index15.curry)(_show.show, instance);
  instance.close = (0, _index15.curry)(close, instance);
  instance.nextOrClose = (0, _index15.curry)(_nextOrClose.nextOrClose, instance);

  instance.element = (0, _index14.update)((0, _index3.element)('section'), {
    class: 'modal anchor'
  });

  instance.h1 = (0, _index14.update)((0, _index3.element)('h1'), {
    parent: instance.element,
    text: (0, _index10.data)(handle.element, 'h1')
  });

  (0, _index10.data)(handle.element, 'h1', null);

  instance.closer = (0, _index14.update)((0, _index3.element)('button'), {
    class: 'closer',
    text: '×',
    onClick: instance.close,
    parent: instance.element
  });

  instance.labels = [];
  instance.inputs = [];

  instance.labels.text = (0, _index14.update)((0, _index3.element)('label'), {
    parent: instance.element,
    text: (0, _index10.data)(handle.element, 'text')
  });
  instance.labels.push(instance.labels.text);

  (0, _index10.data)(handle.element, 'text', null);

  instance.inputs.text = (0, _index14.update)((0, _index3.element)('input'), {
    parent: instance.labels.text,
    value: (0, _index13.text)(handle.element),
    onKeyUp: function onKeyUp(event) {
      (0, _index11.html)(handle.element, event.target.value);
    }
  });
  instance.inputs.push(instance.inputs.text);

  (0, _index.onEnter)(instance.inputs.text, instance.nextOrClose);

  instance.labels.title = (0, _index14.update)((0, _index3.element)('label'), {
    parent: instance.element,
    text: (0, _index10.data)(handle.element, 'title')
  });
  instance.labels.push(instance.labels.title);

  (0, _index10.data)(handle.element, 'title', null);

  instance.inputs.title = (0, _index14.update)((0, _index3.element)('input'), {
    parent: instance.labels.title,
    value: handle.element.title,
    onKeyUp: function onKeyUp(event) {
      (0, _index9.attr)(handle.element, 'title', event.target.value);
    }
  });
  instance.inputs.push(instance.inputs.title);

  (0, _index.onEnter)(instance.inputs.title, instance.nextOrClose);

  instance.labels.href = (0, _index14.update)((0, _index3.element)('label'), {
    parent: instance.element,
    text: (0, _index10.data)(handle.element, 'href')
  });
  instance.labels.push(instance.labels.href);

  (0, _index10.data)(handle.element, 'href', null);

  instance.inputs.href = (0, _index14.update)((0, _index3.element)('input'), {
    parent: instance.labels.href,
    value: (0, _index9.attr)(handle.element, 'href'),
    onKeyUp: function onKeyUp(event) {
      (0, _index9.attr)(handle.element, 'href', event.target.value);
    }
  });
  instance.inputs.push(instance.inputs.href);

  (0, _index.onEnter)(instance.inputs.href, instance.nextOrClose);

  return instance;
}

function close(tooltip) {
  var inputs = tooltip.inputs,
      text = inputs.text.value,
      title = inputs.title.value,
      href = inputs.href.value;

  if (!text.length || !title.length || !href.length) {
    (0, _index12.remove)(tooltip.handle.element);
  }

  (0, _index12.remove)(tooltip.element);
}

/***/ }),
/* 165 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _listenFigure = __webpack_require__(166);

_anticore.anticore.on('figure', function (element, next) {
  (0, _listenFigure.listenFigure)(element);
  next();
});

/***/ }),
/* 166 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.listenFigure = listenFigure;

var _onClick = __webpack_require__(2);

var _figure = __webpack_require__(167);

function listenFigure(element) {
  (0, _onClick.onClick)(element, (0, _figure.figure)(element));
}

/***/ }),
/* 167 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.figure = figure;

var _index = __webpack_require__(26);

var _index2 = __webpack_require__(22);

var _index3 = __webpack_require__(35);

var _index4 = __webpack_require__(6);

var _index5 = __webpack_require__(4);

var _index6 = __webpack_require__(168);

var _index7 = __webpack_require__(31);

var _index8 = __webpack_require__(43);

var _index9 = __webpack_require__(48);

var _index10 = __webpack_require__(5);

var _index11 = __webpack_require__(36);

var _index12 = __webpack_require__(13);

var _index13 = __webpack_require__(14);

var _fromInput = __webpack_require__(75);

var _listenP = __webpack_require__(39);

var _show = __webpack_require__(54);

var _nextOrClose = __webpack_require__(55);

var _onFile = __webpack_require__(79);

function figure(element) {
  var instance = (0, _index13.create)(),
      selection = (0, _index5.current)(),
      target = (0, _index3.closestOrSelf)('[contenteditable=true]', selection.anchorNode);

  instance.element = element;
  instance.caption = (0, _index4.one)('figcaption', element);
  instance.img = (0, _index4.one)('img', element);
  instance.tooltip = tooltip(instance);

  (0, _index6.after)(element, target);

  instance.tooltip.show();

  return instance.tooltip.show;
}

function tooltip(handle) {
  var instance = (0, _index13.create)(),
      options = (0, _index13.create)();

  options.mime = 'image/jpeg';
  options.maxSize = 35000;
  options.quality = 1;
  options.width = 400;
  options.minHeight = 200;

  instance.handle = handle;
  instance.show = (0, _index12.curry)(_show.show, instance);
  instance.close = (0, _index12.curry)(close, instance);
  instance.nextOrClose = (0, _index12.curry)(_nextOrClose.nextOrClose, instance);

  instance.element = (0, _index11.update)((0, _index2.element)('section'), {
    class: 'modal figure'
  });

  instance.h1 = (0, _index11.update)((0, _index2.element)('h1'), {
    parent: instance.element,
    text: (0, _index8.data)(handle.element, 'h1')
  });

  (0, _index8.data)(handle.element, 'h1', null);

  instance.closer = (0, _index11.update)((0, _index2.element)('button'), {
    class: 'closer',
    text: '×',
    onClick: instance.close,
    parent: instance.element
  });

  instance.labels = [];
  instance.inputs = [];

  instance.labels.text = (0, _index11.update)((0, _index2.element)('label'), {
    parent: instance.element,
    text: (0, _index8.data)(handle.caption, 'text')
  });
  instance.labels.push(instance.labels.text);

  (0, _index8.data)(handle.caption, 'text', null);

  instance.inputs.text = (0, _index11.update)((0, _index2.element)('input'), {
    parent: instance.labels.text,
    onKeyUp: function onKeyUp(event) {
      (0, _index9.html)(handle.caption, event.target.value);
    }
  });
  instance.inputs.push(instance.inputs.text);

  (0, _index.onEnter)(instance.inputs.text, instance.nextOrClose);

  instance.labels.alt = (0, _index11.update)((0, _index2.element)('label'), {
    parent: instance.element,
    text: (0, _index8.data)(handle.img, 'alt')
  });
  instance.labels.push(instance.labels.alt);

  (0, _index8.data)(handle.img, 'alt', null);

  instance.inputs.alt = (0, _index11.update)((0, _index2.element)('input'), {
    parent: instance.labels.alt,
    onKeyUp: function onKeyUp(event) {
      (0, _index7.attr)(handle.img, 'alt', event.target.value);
    }
  });
  instance.inputs.push(instance.inputs.alt);

  (0, _index.onEnter)(instance.inputs.alt, instance.nextOrClose);

  instance.labels.src = (0, _index11.update)((0, _index2.element)('label'), {
    parent: instance.element,
    text: (0, _index8.data)(handle.img, 'src')
  });
  instance.labels.push(instance.labels.src);

  (0, _index8.data)(handle.img, 'src', null);

  instance.inputs.src = (0, _index11.update)((0, _index2.element)('input'), {
    parent: instance.labels.src,
    type: 'file',
    onKeyUp: function onKeyUp(event) {
      (0, _index7.attr)(handle.img, 'href', event.target.value);
    }
  });
  instance.inputs.push(instance.inputs.src);

  (0, _fromInput.fromInput)((0, _index12.curry)(_onFile.onFile, instance, options), instance.inputs.src);
  (0, _index.onEnter)(instance.inputs.src, instance.nextOrClose);

  return instance;
}

function close(tooltip) {
  var inputs = tooltip.inputs,
      text = inputs.text.value,
      alt = inputs.alt.value,
      src = inputs.src.value,
      p = void 0;

  if (!text.length || !alt.length || !src.length) {
    (0, _index10.remove)(tooltip.handle.element);
  } else {
    p = (0, _index2.element)('p');

    (0, _listenP.listenP)(p);

    (0, _index6.after)((0, _index11.update)(p, {
      contenteditable: true
    }), tooltip.handle.element);
  }

  (0, _index10.remove)(tooltip.element);
}

/***/ }),
/* 168 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.after = after;

var _before = __webpack_require__(15);

var _nextNode = __webpack_require__(49);

var _parent = __webpack_require__(3);

function after(node, refNode) {
  return (0, _before.before)(node, (0, _nextNode.nextNode)(refNode), (0, _parent.parent)(refNode));
}

/***/ }),
/* 169 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _end = __webpack_require__(58);

var _listenH = __webpack_require__(72);

_anticore.anticore.on('section h1[contenteditable=true]', function (element, next) {
  (0, _listenH.listenH1)(element);
  setTimeout(_end.end, 0, element);

  next();
});

/***/ }),
/* 170 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _listenP = __webpack_require__(39);

_anticore.anticore.on('section.content p[contenteditable=true]', function (element, next) {
  (0, _listenP.listenP)(element);
  next();
});

/***/ }),
/* 171 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


__webpack_require__(172);

__webpack_require__(179);

__webpack_require__(188);

/***/ }),
/* 172 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


__webpack_require__(173);

__webpack_require__(174);

var _anticore = __webpack_require__(0);

var _one = __webpack_require__(6);

var _append = __webpack_require__(19);

var _before = __webpack_require__(15);

_anticore.anticore.on('section.quiz', function (element, next) {
  var footer = (0, _one.one)('form article footer'),
      quiz = (0, _one.one)('section.quiz', footer);

  if (quiz) {
    (0, _append.append)((0, _one.one)('details', element), quiz);
  } else {
    (0, _before.before)(element, (0, _one.one)('section.sources, section.tags', footer));
  }

  next();
});

/***/ }),
/* 173 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _listenP = __webpack_require__(39);

_anticore.anticore.on('section.quiz p[contenteditable=true]', function (element, next) {
  (0, _listenP.listenP)(element);
  next();
});

/***/ }),
/* 174 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _listenSummary = __webpack_require__(175);

_anticore.anticore.on('section.quiz summary', function (element, next) {
  (0, _listenSummary.listenSummary)(element);

  next();
});

/***/ }),
/* 175 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.listenSummary = listenSummary;

var _onBlur = __webpack_require__(56);

var _onEnter = __webpack_require__(26);

var _onShiftEnter = __webpack_require__(73);

var _onClick = __webpack_require__(2);

var _onSpace = __webpack_require__(176);

var _prevent = __webpack_require__(1);

var _index = __webpack_require__(177);

var _closest = __webpack_require__(11);

var _one = __webpack_require__(6);

var _attr = __webpack_require__(31);

var _text = __webpack_require__(12);

var _clean = __webpack_require__(7);

var _remove = __webpack_require__(5);

var _end = __webpack_require__(58);

var spaceFired = false;

function listenSummary(element) {
  (0, _onEnter.onEnter)(element, onEnterEvent, true);
  (0, _onShiftEnter.onShiftEnter)(element, _prevent.prevent, true);
  (0, _onBlur.onBlur)(element, removeDetails, true);
  (0, _onSpace.onSpace)(element, onSpaceEvent, true);
  (0, _onClick.onClick)(element, onClickEvent, true);
}

function onSpaceEvent(event) {
  spaceFired = true;
  (0, _prevent.prevent)(event);
  (0, _index.insert)(' ');
}

function onClickEvent(event) {
  if (spaceFired) {
    (0, _prevent.prevent)(event);
    spaceFired = false;

    return false;
  }
}

function onEnterEvent(event) {
  var target = event.target,
      details = (0, _closest.closest)('details', target);

  (0, _prevent.prevent)(event);
  (0, _attr.attr)(details, 'open', true);
  (0, _end.end)((0, _one.one)('summary + [contenteditable=true]', details));
}

function removeDetails(event) {
  var target = event.target,
      details = (0, _closest.closest)('details', target),
      section = (0, _closest.closest)('section', details);

  (0, _text.text)(target, (0, _text.text)(target).replace(/ /g, ' '));
  (0, _clean.clean)(section);

  if (!(0, _text.text)(details).trim()) {
    (0, _remove.remove)(details);
  }

  if (!(0, _one.one)('details', section)) {
    (0, _remove.remove)(section);
  }
}

/***/ }),
/* 176 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onSpace = onSpace;

var _ = __webpack_require__(8);

function onSpace(element, listener, useCapture) {
  return (0, _.on)('keydown', element, function (event) {
    if (event.which === 32 && !event.shiftKey && !event.ctrlKey) {
      return listener.call(this, event);
    }
  }, useCapture);
}

/***/ }),
/* 177 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.insert = insert;

var _isBoolean = __webpack_require__(69);

var _isNumber = __webpack_require__(42);

var _isString = __webpack_require__(41);

var _text = __webpack_require__(178);

var _current = __webpack_require__(4);

function insert(node) {
  var selection = (0, _current.current)(),
      range = selection.getRangeAt(0);

  if ((0, _isString.isString)(node) || (0, _isNumber.isNumber)(node) || (0, _isBoolean.isBoolean)(node)) {
    node = (0, _text.text)(node);
  }

  range.deleteContents();
  range.insertNode(node);
  selection.collapseToEnd();

  return selection;
}

/***/ }),
/* 178 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.text = text;

var _document = __webpack_require__(20);

function text(data) {
  return (0, _document.document)().createTextNode(data);
}

/***/ }),
/* 179 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


__webpack_require__(180);

var _anticore = __webpack_require__(0);

var _one = __webpack_require__(6);

var _append = __webpack_require__(19);

var _before = __webpack_require__(15);

_anticore.anticore.on('section.sources', function (element, next) {
  var footer = (0, _one.one)('form article footer'),
      sources = (0, _one.one)('section.sources', footer);

  if (sources) {
    (0, _append.append)((0, _one.one)('li', element), (0, _one.one)('ol', sources));
  } else {
    (0, _before.before)(element, (0, _one.one)('section.tags', footer));
  }

  next();
});

/***/ }),
/* 180 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _index = __webpack_require__(16);

var _index2 = __webpack_require__(6);

var _text = __webpack_require__(12);

var _attr = __webpack_require__(31);

var _appendAll = __webpack_require__(28);

var _listenLi = __webpack_require__(59);

var _forEach = __webpack_require__(21);

_anticore.anticore.on('section.sources li[contenteditable=true]', function (element, next) {
  var anchors = (0, _index.all)('article a[href^="#"]'),
      items = (0, _index.all)('article footer .sources li'),
      ordered = [],
      index = 0;

  (0, _forEach.forEach)(anchors, function (anchor, key) {
    var item = element,
        id = 'source'.concat(key + 1);

    if ((0, _attr.attr)(anchor, 'href') !== '#') {
      item = items[index];
      index += 1;
    }

    (0, _text.text)((0, _index2.one)('sup', anchor), key + 1);
    anchor.href = '#'.concat(id);
    item.id = id;
    ordered.push(item);
  });

  (0, _listenLi.listenLi)(element);

  setTimeout(function () {
    (0, _appendAll.appendAll)(ordered, (0, _index2.one)('article footer .sources ol'));
  }, 0);

  next();
});

/***/ }),
/* 181 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onBlurEvent = onBlurEvent;

var _clean = __webpack_require__(7);

var _remove = __webpack_require__(5);

var _isFirstLi = __webpack_require__(90);

function onBlurEvent(event) {
  var target = event.target;

  if (!(0, _clean.clean)(target) || (0, _isFirstLi.isFirstLi)(target)) {
    return;
  }

  (0, _remove.remove)(target);
}

/***/ }),
/* 182 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onEnterEvent = onEnterEvent;

var _anchor = __webpack_require__(33);

var _isCollapsed = __webpack_require__(34);

var _start = __webpack_require__(53);

var _starts = __webpack_require__(45);

var _before = __webpack_require__(15);

var _isFirstLi = __webpack_require__(90);

var _listenLi = __webpack_require__(59);

var _clean = __webpack_require__(7);

var _cut = __webpack_require__(57);

var _elements = __webpack_require__(183);

var _matches = __webpack_require__(23);

var _nextElement = __webpack_require__(40);

var _parent = __webpack_require__(3);

var _prevent = __webpack_require__(1);

var _isEmpty = __webpack_require__(25);

function onEnterEvent(event) {
  var target = event.target,
      node = _anchor.anchor.node(),
      offset = _anchor.anchor.offset(),
      rest = void 0;

  (0, _prevent.prevent)(event);

  if (isInvalid(target)) {
    return;
  }

  rest = (0, _cut.cut)(node, offset);
  rest.normalize();
  (0, _clean.clean)(target);
  (0, _clean.clean)(rest);
  (0, _listenLi.listenLi)(rest);
  (0, _before.before)(rest, (0, _nextElement.nextElement)(target), (0, _parent.parent)(target));
  (0, _start.start)(rest);
}

function isInvalid(target) {
  var node = _anchor.anchor.node(),
      offset = _anchor.anchor.offset(),
      list = (0, _parent.parent)(target);

  return !(0, _isCollapsed.isCollapsed)() || (0, _isEmpty.isEmpty)(target) || (0, _isEmpty.isEmpty)(node) || (0, _isFirstLi.isFirstLi)(target) && (0, _starts.starts)(target) || !offset && node === target || (0, _matches.matches)('form article > footer > .tags ul', list) && (0, _elements.elements)(list).length > 5;
}

/***/ }),
/* 183 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.elements = elements;
function elements(node) {
  return node.children;
}

/***/ }),
/* 184 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onBackEvent = onBackEvent;

var _current = __webpack_require__(4);

var _previous = __webpack_require__(46);

var _starts = __webpack_require__(45);

var _appendAll = __webpack_require__(28);

var _clean = __webpack_require__(7);

var _remove = __webpack_require__(5);

var _isText = __webpack_require__(24);

var _isFirstP = __webpack_require__(44);

var _isHeadingNode = __webpack_require__(30);

var _editables = __webpack_require__(27);

var _parent = __webpack_require__(3);

var _nodes = __webpack_require__(17);

var _prevent = __webpack_require__(1);

function onBackEvent(event) {
  var target = event.target,
      selection = (0, _current.current)(),
      anchor = selection.anchorNode,
      node = void 0;

  if (!selection.isCollapsed) {
    return;
  }

  if (!(0, _starts.starts)(target)) {
    return;
  }

  if (!(0, _isFirstP.isFirstP)(target)) {
    node = (0, _previous.previous)(target, (0, _editables.editables)(target));

    if (node && !(0, _isHeadingNode.isHeadingNode)(node) && anchor) {
      if ((0, _isText.isText)(node)) {
        node = (0, _parent.parent)(node);
      }

      (0, _appendAll.appendAll)((0, _nodes.nodes)(target), node);
      (0, _clean.clean)(node);
      (0, _remove.remove)(target);
    }
  }

  (0, _prevent.prevent)(event);
}

/***/ }),
/* 185 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onDeleteEvent = onDeleteEvent;

var _isCollapsedToNext = __webpack_require__(89);

var _clean = __webpack_require__(7);

var _remove = __webpack_require__(5);

var _appendAll = __webpack_require__(28);

var _nextElement = __webpack_require__(40);

var _nodes = __webpack_require__(17);

var _prevent = __webpack_require__(1);

function onDeleteEvent(event) {
  var target = event.target,
      next = (0, _nextElement.nextElement)(target);

  if ((0, _isCollapsedToNext.isCollapsedToNext)(event)) {
    (0, _appendAll.appendAll)((0, _nodes.nodes)(next), target);
    (0, _clean.clean)(target);
    (0, _remove.remove)(next);
    (0, _prevent.prevent)(event);

    return;
  }
}

/***/ }),
/* 186 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onLeftEvent = onLeftEvent;

var _current = __webpack_require__(4);

var _previous = __webpack_require__(46);

var _clean = __webpack_require__(7);

var _remove = __webpack_require__(5);

var _firstNode = __webpack_require__(37);

var _editables = __webpack_require__(27);

var _indexOf = __webpack_require__(10);

var _prevent = __webpack_require__(1);

function onLeftEvent(event) {
  var target = event.target,
      selection = (0, _current.current)(),
      anchor = selection.anchorNode,
      offset = selection.anchorOffset;

  if (!(0, _current.current)().isCollapsed) {
    return;
  }

  if ((0, _indexOf.indexOf)([target, (0, _firstNode.firstNode)(target)], anchor) > -1 && !offset) {
    (0, _previous.previous)(target, (0, _editables.editables)(target));

    if ((0, _clean.clean)(target)) {
      (0, _remove.remove)(target);
    }

    (0, _prevent.prevent)(event);
  }
}

/***/ }),
/* 187 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.onRightEvent = onRightEvent;

var _current = __webpack_require__(4);

var _next = __webpack_require__(52);

var _clean = __webpack_require__(7);

var _remove = __webpack_require__(5);

var _text = __webpack_require__(12);

var _indexOf = __webpack_require__(10);

var _editables = __webpack_require__(27);

var _prevent = __webpack_require__(1);

function onRightEvent(event) {
  var target = event.target,
      selection = (0, _current.current)(),
      anchor = selection.anchorNode,
      offset = selection.anchorOffset;

  if (!(0, _current.current)().isCollapsed) {
    return;
  }

  if ((0, _indexOf.indexOf)([target, target.lastChild], anchor) > -1 && offset === (0, _text.text)(anchor).length) {
    (0, _next.next)(target, (0, _editables.editables)(target));

    if ((0, _clean.clean)(target)) {
      (0, _remove.remove)(target);
    }

    (0, _prevent.prevent)(event);
  }
}

/***/ }),
/* 188 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


__webpack_require__(189);

/***/ }),
/* 189 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _anticore = __webpack_require__(0);

var _listenLi = __webpack_require__(59);

_anticore.anticore.on('section.tags li[contenteditable=true]', function (element, next) {
  (0, _listenLi.listenLi)(element);
  next();
});

/***/ })
/******/ ]);